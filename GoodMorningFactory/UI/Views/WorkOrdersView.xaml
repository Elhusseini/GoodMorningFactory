<!-- UI/Views/WorkOrdersView.xaml -->
<!-- *** تحديث: تم تبسيط الـ Triggers لإظهار الأزرار *** -->
<UserControl x:Class="GoodMorningFactory.UI.Views.WorkOrdersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000"
             Background="#F5F5F5" FlowDirection="RightToLeft">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Button Content="➕ إنشاء أمر عمل جديد" Grid.Row="0" HorizontalAlignment="Left" Margin="5" Padding="10,5" Click="AddWorkOrderButton_Click"/>

        <DataGrid x:Name="WorkOrdersDataGrid" Grid.Row="1" Margin="5" IsReadOnly="True" AutoGenerateColumns="False" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="رقم الأمر" Binding="{Binding WorkOrderNumber}" Width="*"/>
                <DataGridTextColumn Header="المنتج" Binding="{Binding FinishedGood.Name}" Width="2*"/>
                <DataGridTextColumn Header="الكمية المطلوبة" Binding="{Binding QuantityToProduce}" Width="*"/>
                <DataGridTextColumn Header="الكمية المنتجة" Binding="{Binding QuantityProduced}" Width="*"/>
                <DataGridTextColumn Header="تاريخ البدء المخطط" Binding="{Binding PlannedStartDate, StringFormat='d'}" Width="*"/>
                <DataGridTextColumn Header="الحالة" Binding="{Binding Status}" Width="*" FontWeight="Bold"/>
                <DataGridTemplateColumn Header="إجراءات" Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="عرض/تعديل" Margin="2" Click="ViewDetailsButton_Click"/>
                                <Button Content="بدء التنفيذ" Margin="2" Click="StartButton_Click" Background="LightGreen">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Status}" Value="Planned">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                                <!-- *** بداية التحديث: إظهار الأزرار للأوامر قيد التنفيذ *** -->
                                <Button Content="صرف مواد" Margin="2" Click="ConsumeMaterialsButton_Click" Background="LightSalmon">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Status}" Value="InProgress">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                                <Button Content="تسجيل إنتاج" Margin="2" Click="ReportProductionButton_Click" Background="LightSkyBlue">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Status}" Value="InProgress">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                                <!-- *** نهاية التحديث *** -->
                                <Button Content="إلغاء" Margin="2" Click="CancelButton_Click" Background="OrangeRed" Foreground="White"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>