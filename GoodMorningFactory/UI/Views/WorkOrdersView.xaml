<UserControl x:Class="GoodMorningFactory.UI.Views.WorkOrdersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:vm="clr-namespace:GoodMorningFactory.UI.ViewModels"
             xmlns:converters="clr-namespace:GoodMorningFactory.UI.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="950"
             Background="#F4F7FC"
             FlowDirection="RightToLeft"
             FontFamily="Segoe UI"
             d:DataContext="{d:DesignInstance Type=vm:WorkOrdersViewModel, IsDesignTimeCreatable=True}">
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding LoadDataCommand}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>
    <UserControl.Resources>
        <converters:EnumToDescriptionConverter x:Key="EnumToDescriptionConverter"/>
        <Style TargetType="Button" x:Key="ActionButtonStyle">
            <Setter Property="Background" Value="#4A5568"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="FontSize" Value="11"/>
        </Style>
        <Style TargetType="Button" BasedOn="{StaticResource ActionButtonStyle}" x:Key="StartButtonStyle">
            <Setter Property="Background" Value="#38A169"/>
        </Style>
        <Style TargetType="Button" BasedOn="{StaticResource ActionButtonStyle}" x:Key="ConsumeButtonStyle">
            <Setter Property="Background" Value="#DD6B20"/>
        </Style>
        <Style TargetType="Button" BasedOn="{StaticResource ActionButtonStyle}" x:Key="ReportButtonStyle">
            <Setter Property="Background" Value="#3182CE"/>
        </Style>
        <Style TargetType="Button" BasedOn="{StaticResource ActionButtonStyle}" x:Key="CancelButtonStyle">
            <Setter Property="Background" Value="#E53E3E"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="إدارة أوامر العمل" FontSize="28" FontWeight="Bold" Foreground="#1A202C" Margin="0,0,0,20"/>
        <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="15" Margin="0,0,0,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="بحث:" VerticalAlignment="Center" Margin="5"/>
                    <TextBox Width="200" Margin="5" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged, Delay=300}"/>
                    <TextBlock Text="الحالة:" VerticalAlignment="Center" Margin="10,0,0,0"/>
                    <ComboBox Width="150" Margin="5" ItemsSource="{Binding StatusFilters}" SelectedItem="{Binding SelectedStatus}">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Converter={StaticResource EnumToDescriptionConverter}}"/>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                </StackPanel>
                <Button Grid.Column="1" Content="إضافة أمر عمل جديد" Command="{Binding AddWorkOrderCommand}" Background="#38A169"/>
            </Grid>
        </Border>
        <DataGrid Grid.Row="2" ItemsSource="{Binding WorkOrders}" Margin="0" IsReadOnly="True" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="رقم الأمر" Binding="{Binding WorkOrderNumber}" Width="*"/>
                <DataGridTextColumn Header="المنتج" Binding="{Binding FinishedGoodName}" Width="2*"/>
                <DataGridTextColumn Header="الكمية المطلوبة" Binding="{Binding QuantityToProduce}" Width="*"/>
                <DataGridTextColumn Header="الكمية المنتجة" Binding="{Binding QuantityProduced}" Width="*"/>
                <DataGridTextColumn Header="تاريخ البدء المخطط" Binding="{Binding PlannedStartDate, StringFormat=d}" Width="*"/>
                <DataGridTextColumn Header="الحالة" Binding="{Binding Status, Converter={StaticResource EnumToDescriptionConverter}}" Width="*" FontWeight="SemiBold"/>
                <DataGridTemplateColumn Header="إجراءات" Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="عرض/تعديل" Style="{StaticResource ActionButtonStyle}" Command="{Binding DataContext.ViewDetailsCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}"/>
                                <Button Content="بدء التنفيذ" Style="{StaticResource StartButtonStyle}" Command="{Binding DataContext.StartCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}"/>
                                <Button Content="صرف مواد" Style="{StaticResource ConsumeButtonStyle}" Command="{Binding DataContext.ConsumeMaterialsCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}"/>
                                <Button Content="تسجيل إنتاج" Style="{StaticResource ReportButtonStyle}" Command="{Binding DataContext.ReportProductionCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}"/>
                                <Button Content="تسجيل عمالة" Style="{StaticResource ActionButtonStyle}" Command="{Binding DataContext.RecordLaborCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}"/>
                                <Button Content="إلغاء" Style="{StaticResource CancelButtonStyle}" Command="{Binding DataContext.CancelCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
            <Button Content="→ السابق" Margin="5,0" Command="{Binding PreviousPageCommand}"/>
            <TextBlock Text="{Binding PageInfo}" VerticalAlignment="Center" FontWeight="SemiBold"/>
            <Button Content="التالي ←" Margin="5,0" Command="{Binding NextPageCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>