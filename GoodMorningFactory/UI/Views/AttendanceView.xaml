<UserControl x:Class="GoodMorningFactory.UI.Views.AttendanceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:GoodMorningFactory.UI.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000"
             Background="#F5F5F5" FlowDirection="RightToLeft">

    <UserControl.DataContext>
        <vm:AttendanceViewModel/>
    </UserControl.DataContext>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <ToolBarTray Grid.Row="0">
            <ToolBar>
                <Button Content="➕ تسجيل يدوي" Margin="5" Padding="5" Command="{Binding AddManualAttendanceCommand}"/>
                <Separator/>
                <TextBlock Text="عرض سجلات تاريخ:" VerticalAlignment="Center" Margin="5"/>
                <DatePicker Width="150" SelectedDate="{Binding SelectedDate, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            </ToolBar>
        </ToolBarTray>

        <DataGrid ItemsSource="{Binding AttendanceSummary}" Grid.Row="1" Margin="5" IsReadOnly="True" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="الموظف" Binding="{Binding EmployeeName}" Width="2*"/>
                <DataGridTextColumn Header="وقت الحضور" Binding="{Binding TimeIn}" Width="*"/>
                <DataGridTextColumn Header="وقت الانصراف" Binding="{Binding TimeOut}" Width="*"/>
                <DataGridTextColumn Header="ساعات العمل" Binding="{Binding HoursWorked}" Width="*"/>
                <DataGridTextColumn Header="الحالة" Binding="{Binding Status}" Width="*" FontWeight="Bold"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>