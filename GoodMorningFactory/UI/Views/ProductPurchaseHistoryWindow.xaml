<Window x:Class="GoodMorningFactory.UI.Views.ProductPurchaseHistoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:vm="clr-namespace:GoodMorningFactory.UI.ViewModels"
        mc:Ignorable="d"
        Title="{Binding WindowTitle}" Height="500" Width="800"
        WindowStartupLocation="CenterScreen" FlowDirection="RightToLeft"
        d:DataContext="{d:DesignInstance Type=vm:ProductPurchaseHistoryViewModel, IsDesignTimeCreatable=True}">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Margin="0,0,0,10">
            <Run Text="سجل أسعار الشراء للمنتج:" FontSize="18"/>
            <Run Text="{Binding ProductName}" FontSize="18" FontWeight="Bold" Foreground="DarkBlue"/>
        </TextBlock>
        <DataGrid Grid.Row="1" ItemsSource="{Binding PurchaseHistory}" IsReadOnly="True" AutoGenerateColumns="False" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="تاريخ الفاتورة" Binding="{Binding PurchaseDate, StringFormat='yyyy/MM/dd'}" Width="*"/>
                <DataGridTextColumn Header="رقم فاتورة المورد" Binding="{Binding InvoiceNumber}" Width="*"/>
                <DataGridTextColumn Header="اسم المورد" Binding="{Binding SupplierName}" Width="2*"/>
                <DataGridTextColumn Header="الكمية المشتراة" Binding="{Binding Quantity}" Width="*"/>
                <DataGridTextColumn Header="سعر الوحدة" Binding="{Binding UnitPriceFormatted}" Width="*" FontWeight="Bold"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>