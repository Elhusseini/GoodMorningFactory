<UserControl x:Class="GoodMorningFactory.UI.Views.InventoryCountsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:GoodMorningFactory.UI.ViewModels"
             xmlns:converters="clr-namespace:GoodMorningFactory.UI.Converters"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800" FlowDirection="RightToLeft"
             d:DataContext="{d:DesignInstance Type=vm:InventoryCountsViewModel, IsDesignTimeCreatable=True}">

	<UserControl.Resources>
		<converters:EnumToDescriptionConverter x:Key="EnumToDescriptionConverter"/>
	</UserControl.Resources>

	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>

		<TextBlock Text="إدارة أوامر الجرد" Style="{StaticResource TitleTextBlockStyle}" Grid.Row="0"/>

		<Border Grid.Row="1" Style="{StaticResource SectionBorderStyle}">
			<StackPanel Orientation="Vertical">
				<WrapPanel Margin="5">
					<Button Content="أمر جرد جديد" Margin="5" Command="{Binding AddCountCommand}" Style="{StaticResource PrimaryButtonStyle}"/>
					<Button Content="تحديث القائمة" Margin="5" Command="{Binding RefreshCommand}"/>
				</WrapPanel>
				<Separator/>
				<WrapPanel Margin="5">
					<TextBlock Text="بحث:" VerticalAlignment="Center" Margin="5"/>
					<TextBox Width="200" Margin="5" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged, Delay=300}"/>
					<TextBlock Text="الحالة:" VerticalAlignment="Center" Margin="15,5,5,5"/>
					<ComboBox Width="150" Margin="5" ItemsSource="{Binding StatusFilters}" SelectedItem="{Binding SelectedStatus}">
						<ComboBox.ItemTemplate>
							<DataTemplate>
								<TextBlock Text="{Binding Converter={StaticResource EnumToDescriptionConverter}}"/>
							</DataTemplate>
						</ComboBox.ItemTemplate>
					</ComboBox>
				</WrapPanel>
			</StackPanel>
		</Border>

		<DataGrid ItemsSource="{Binding Counts}" Grid.Row="2" Style="{StaticResource DataGridStyle}" CanUserAddRows="False">
			<DataGrid.Columns>
				<DataGridTextColumn Header="الرقم المرجعي" Binding="{Binding CountReferenceNumber}" Width="*"/>
				<DataGridTextColumn Header="تاريخ الجرد" Binding="{Binding CountDate, StringFormat='yyyy/MM/dd'}" Width="*"/>
				<DataGridTextColumn Header="المخزن" Binding="{Binding WarehouseName}" Width="*"/>
				<DataGridTextColumn Header="المسؤول" Binding="{Binding ResponsibleUser}" Width="*"/>
				<DataGridTextColumn Header="الحالة" Binding="{Binding Status, Converter={StaticResource EnumToDescriptionConverter}}" Width="*"/>
				<DataGridTemplateColumn Header="إجراءات" Width="Auto">
					<DataGridTemplateColumn.CellTemplate>
						<DataTemplate>
							<StackPanel Orientation="Horizontal">
								<Button Content="عرض/تعديل" Margin="2" Command="{Binding DataContext.ViewDetailsCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}"/>
								<Button Content="إلغاء" Margin="2" Command="{Binding DataContext.CancelCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}"/>
							</StackPanel>
						</DataTemplate>
					</DataGridTemplateColumn.CellTemplate>
				</DataGridTemplateColumn>
			</DataGrid.Columns>
		</DataGrid>

		<StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="5">
			<Button Content="السابق" Margin="5" Command="{Binding PreviousPageCommand}"/>
			<TextBlock VerticalAlignment="Center" Margin="10,0" Text="{Binding PageInfo}"/>
			<Button Content="التالي" Margin="5" Command="{Binding NextPageCommand}"/>
		</StackPanel>
	</Grid>
</UserControl>