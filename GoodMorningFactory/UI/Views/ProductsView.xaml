<UserControl x:Class="GoodMorningFactory.UI.Views.ProductsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:GoodMorningFactory.UI.ViewModels"
             xmlns:converters="clr-namespace:GoodMorningFactory.UI.Converters"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="1100"
             FlowDirection="RightToLeft"
             Background="#F5F5F5"
             d:DataContext="{d:DesignInstance Type=vm:ProductsViewViewModel, IsDesignTimeCreatable=True}">

	<UserControl.Resources>
		<converters:EnumToDescriptionConverter x:Key="EnumToDescriptionConverter"/>
		<converters:BooleanToStatusConverter x:Key="BooleanToStatusConverter"/>
		<converters:BoolToColorConverter x:Key="BoolToColorConverter"/>
		<converters:StockStatusToColorConverter x:Key="StockStatusToColorConverter"/>
	</UserControl.Resources>

	<Grid Margin="15">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>

		<StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
			<TextBlock Text="إدارة المنتجات" FontSize="24" FontWeight="Bold" VerticalAlignment="Center"/>
			<Button Content="➕ إضافة منتج" Margin="20,0,0,0" Command="{Binding AddProductCommand}"/>
			<Button Content="📁 تصدير إلى CSV" Margin="10,0,0,0" Command="{Binding ExportToCsvCommand}" Background="#107C10" Foreground="White"/>
		</StackPanel>

		<Border Grid.Row="1" Background="White" CornerRadius="5" Padding="10" Margin="0,0,0,10" BorderBrush="#DDD" BorderThickness="1">
			<WrapPanel Orientation="Horizontal">
				<StackPanel Margin="5">
					<TextBlock Text="بحث (اسم/كود):" FontWeight="SemiBold" Margin="0,0,0,2"/>
					<TextBox Width="200" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged, Delay=300}"/>
				</StackPanel>
				<StackPanel Margin="5">
					<TextBlock Text="الفئة:" FontWeight="SemiBold" Margin="0,0,0,2"/>
					<ComboBox Width="150" ItemsSource="{Binding CategoryFilters}" SelectedItem="{Binding SelectedCategory}" DisplayMemberPath="Name"/>
				</StackPanel>
				<StackPanel Margin="5">
					<TextBlock Text="المورد:" FontWeight="SemiBold" Margin="0,0,0,2"/>
					<ComboBox Width="150" ItemsSource="{Binding SupplierFilters}" SelectedItem="{Binding SelectedSupplier}" DisplayMemberPath="Name"/>
				</StackPanel>
				<StackPanel Margin="5">
					<TextBlock Text="النوع:" FontWeight="SemiBold" Margin="0,0,0,2"/>
					<ComboBox Width="120" ItemsSource="{Binding ProductTypeFilters}" SelectedItem="{Binding SelectedProductType}" DisplayMemberPath="Name"/>
				</StackPanel>
				<StackPanel Margin="5">
					<TextBlock Text="الحالة:" FontWeight="SemiBold" Margin="0,0,0,2"/>
					<ComboBox Width="120" ItemsSource="{Binding StatusFilters}" SelectedItem="{Binding SelectedStatus}" DisplayMemberPath="Name"/>
				</StackPanel>
				<StackPanel Margin="15,5,5,5" VerticalAlignment="Bottom" Orientation="Horizontal">
					<Button Content="إدارة الفئات" Margin="5,0" Click="ManageCategoriesButton_Click"/>
					<Button Content="إدارة الوحدات" Margin="5,0" Click="ManageUomButton_Click"/>
					<Button Content="إدارة الأسعار" Margin="5,0" Click="ManagePriceListsButton_Click"/>
				</StackPanel>
			</WrapPanel>
		</Border>

		<DataGrid Grid.Row="2" Margin="0,10,0,0" ItemsSource="{Binding Products}" IsReadOnly="True" AutoGenerateColumns="False" CanUserAddRows="False">
			<DataGrid.Columns>
				<DataGridTemplateColumn Header="صورة" Width="Auto">
					<DataGridTemplateColumn.CellTemplate>
						<DataTemplate>
							<Image Source="{Binding ProductImage}" Height="40" Width="40" Stretch="Uniform" Margin="2"/>
						</DataTemplate>
					</DataGridTemplateColumn.CellTemplate>
				</DataGridTemplateColumn>
				<DataGridTextColumn Header="كود المنتج" Binding="{Binding ProductCode}" Width="*"/>
				<DataGridTextColumn Header="اسم المنتج" Binding="{Binding Name}" Width="2*"/>
				<DataGridTextColumn Header="الفئة" Binding="{Binding CategoryName}" Width="*"/>
				<DataGridTextColumn Header="سعر البيع" Binding="{Binding SalePriceFormatted}" Width="*"/>
				<DataGridTextColumn Header="المخزون" Binding="{Binding CurrentStock}" Width="Auto" FontWeight="Bold"/>
				<DataGridTemplateColumn Header="حالة المخزون" Width="*">
					<DataGridTemplateColumn.CellTemplate>
						<DataTemplate>
							<Border CornerRadius="10" Padding="8,2"
                                    Background="{Binding StockStatus, Converter={StaticResource StockStatusToColorConverter}}">
								<TextBlock Text="{Binding StockStatus, Converter={StaticResource EnumToDescriptionConverter}}"
                                           Foreground="White" FontWeight="SemiBold" HorizontalAlignment="Center"/>
							</Border>
						</DataTemplate>
					</DataGridTemplateColumn.CellTemplate>
				</DataGridTemplateColumn>
				<DataGridTemplateColumn Header="الحالة" Width="*">
					<DataGridTemplateColumn.CellTemplate>
						<DataTemplate>
							<TextBlock Text="{Binding IsActive, Converter={StaticResource BooleanToStatusConverter}}"
                                       Foreground="{Binding IsActive, Converter={StaticResource BoolToColorConverter}}"
                                       FontWeight="Bold" HorizontalAlignment="Center"/>
						</DataTemplate>
					</DataGridTemplateColumn.CellTemplate>
				</DataGridTemplateColumn>
				<DataGridTemplateColumn Header="إجراءات" Width="Auto">
					<DataGridTemplateColumn.CellTemplate>
						<DataTemplate>
							<StackPanel Orientation="Horizontal">
								<Button Content="تعديل" Margin="2" Command="{Binding DataContext.EditProductCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}"/>
								<Button Content="نسخ" Margin="2" Command="{Binding DataContext.DuplicateProductCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}"/>
								<Button Content="إدارة المخزون" Margin="2" Command="{Binding DataContext.ManageStockCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}"/>
								<Button Content="سجل الأسعار" Margin="2" Command="{Binding DataContext.ViewPurchaseHistoryCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}"/>
								<Button Content="حذف" Margin="2" Command="{Binding DataContext.DeleteProductCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" Background="IndianRed" Foreground="White"/>
							</StackPanel>
						</DataTemplate>
					</DataGridTemplateColumn.CellTemplate>
				</DataGridTemplateColumn>
			</DataGrid.Columns>
		</DataGrid>

		<Border Grid.Row="3" BorderThickness="0,1,0,0" BorderBrush="LightGray" Padding="5" Margin="0,10,0,0">
			<Grid>
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="*"/>
					<ColumnDefinition Width="Auto"/>
					<ColumnDefinition Width="*"/>
				</Grid.ColumnDefinitions>
				<StackPanel Grid.Column="0" Orientation="Horizontal" HorizontalAlignment="Left">
					<Button Content="&lt; السابق" Margin="5,0" Command="{Binding PreviousPageCommand}"/>
				</StackPanel>
				<TextBlock Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center" Text="{Binding PageInfo}"/>
				<StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right">
					<Button Content="التالي &gt;" Margin="5,0" Command="{Binding NextPageCommand}"/>
				</StackPanel>
			</Grid>
		</Border>
	</Grid>
</UserControl>