<!-- UI/Views/ProductsView.xaml -->
<!-- *** الكود الكامل لواجهة عرض المنتجات مع الفلاتر والتحسينات *** -->
<UserControl x:Class="GoodMorningFactory.UI.Views.ProductsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="1000"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:converters="clr-namespace:GoodMorningFactory.UI.Converters"
             Background="#F5F5F5" FlowDirection="RightToLeft">
	<UserControl.Resources>
		<converters:BooleanToStatusConverter x:Key="BooleanToStatusConverter"/>
	</UserControl.Resources>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
			<RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ToolBarTray Grid.Row="0">
            <ToolBar>
				<Button Content="➕ إضافة منتج جديد" Margin="5" Padding="5" Click="AddProductButton_Click"/>
                <Separator/>
				<TextBlock Text="بحث:" VerticalAlignment="Center" Margin="5"/>
				<TextBox x:Name="SearchTextBox" Width="200" KeyUp="Filter_KeyUp"/>
				<TextBlock Text="الفئة:" VerticalAlignment="Center" Margin="5"/>
				<ComboBox x:Name="CategoryFilterComboBox" Width="150" SelectionChanged="Filter_Changed" DisplayMemberPath="Name" SelectedValuePath="Id"/>
            </ToolBar>
        </ToolBarTray>

		<DataGrid x:Name="ProductsDataGrid" Grid.Row="1" Margin="5" IsReadOnly="True" AutoGenerateColumns="False">
            <DataGrid.Columns>
				<DataGridTextColumn Header="كود المنتج" Binding="{Binding ProductCode}" Width="*"/>
				<DataGridTextColumn Header="اسم المنتج" Binding="{Binding Name}" Width="2*"/>
				<DataGridTextColumn Header="الفئة" Binding="{Binding CategoryName}" Width="*"/>
				<DataGridTextColumn Header="نوع المنتج" Binding="{Binding ProductType}" Width="*"/>
				<DataGridTextColumn Header="سعر البيع" Binding="{Binding SalePrice, StringFormat=C}" Width="*"/>
				<DataGridTextColumn Header="المخزون الحالي" Binding="{Binding CurrentStock}" Width="*"/>
				<DataGridTextColumn Header="الحالة" Binding="{Binding IsActive, Converter={StaticResource BooleanToStatusConverter}}" Width="*"/>
				<DataGridTemplateColumn Header="إجراءات" Width="Auto">
					<DataGridTemplateColumn.CellTemplate>
						<DataTemplate>
							<StackPanel Orientation="Horizontal">
								<Button Content="تعديل" Margin="2" Click="EditButton_Click"/>
								<Button Content="حذف" Margin="2" Click="DeleteButton_Click" Background="LightCoral"/>
							</StackPanel>
						</DataTemplate>
					</DataGridTemplateColumn.CellTemplate>
				</DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

		<Border Grid.Row="2" BorderThickness="0,1,0,0" BorderBrush="LightGray" Padding="5" Margin="0,5,0,0">
			<!-- (عناصر ترقيم الصفحات سيتم إضافتها هنا) -->
		</Border>
    </Grid>
</UserControl>