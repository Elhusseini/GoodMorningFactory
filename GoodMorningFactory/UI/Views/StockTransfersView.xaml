<UserControl x:Class="GoodMorningFactory.UI.Views.StockTransfersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:GoodMorningFactory.UI.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             FlowDirection="RightToLeft"
             d:DataContext="{d:DesignInstance Type=vm:StockTransfersViewModel, IsDesignTimeCreatable=True}">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="سجل التحويلات المخزنية" Style="{DynamicResource TitleTextBlockStyle}" Grid.Row="0"/>

        <Button Content="إنشاء تحويل جديد" Command="{Binding AddTransferCommand}" Style="{DynamicResource PrimaryButtonStyle}" Grid.Row="1" HorizontalAlignment="Left" Margin="0,0,0,10"/>

        <DataGrid ItemsSource="{Binding Transfers}" Grid.Row="2" IsReadOnly="True" AutoGenerateColumns="False" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="رقم التحويل" Binding="{Binding TransferNumber}" Width="*"/>
                <DataGridTextColumn Header="التاريخ" Binding="{Binding TransferDate, StringFormat='g'}" Width="*"/>
                <DataGridTextColumn Header="من مخزن" Binding="{Binding SourceStorageLocation.Warehouse.Name}" Width="2*"/>
                <DataGridTextColumn Header="إلى مخزن" Binding="{Binding DestinationStorageLocation.Warehouse.Name}" Width="2*"/>
                <DataGridTextColumn Header="المستخدم" Binding="{Binding User.Username}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>