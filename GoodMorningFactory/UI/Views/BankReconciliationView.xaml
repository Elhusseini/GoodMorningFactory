<UserControl x:Class="GoodMorningFactory.UI.Views.BankReconciliationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:GoodMorningFactory.UI.ViewModels"
             mc:Ignorable="d" d:DesignHeight="700" d:DesignWidth="1200"
             Background="#F5F5F5" FlowDirection="RightToLeft">

    <UserControl.DataContext>
        <vm:BankReconciliationViewModel/>
    </UserControl.DataContext>

    <DockPanel Margin="20">
        <Border DockPanel.Dock="Top" Background="White" CornerRadius="5" Margin="0,0,0,15" Padding="15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="15"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="معلومات الحساب" FontWeight="Bold" Margin="0,0,0,10"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="10"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <TextBlock Text="الحساب البنكي:" VerticalAlignment="Center" Grid.Row="0" Grid.Column="0"/>
                        <ComboBox ItemsSource="{Binding BankAccounts}" SelectedItem="{Binding SelectedBankAccount}" DisplayMemberPath="DisplayName" Grid.Row="0" Grid.Column="1" Margin="10,0,0,0"/>
                        <TextBlock Text="رصيد الدفاتر:" VerticalAlignment="Center" Grid.Row="2" Grid.Column="0"/>
                        <TextBlock Text="{Binding BookBalanceFormatted}" Grid.Row="2" Grid.Column="1" Margin="10,0,0,0" FontWeight="Medium"/>
                    </Grid>
                </StackPanel>
                <StackPanel Grid.Column="2">
                    <TextBlock Text="معلومات كشف الحساب" FontWeight="Bold" Margin="0,0,0,10"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="10"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <TextBlock Text="التسوية حتى تاريخ:" VerticalAlignment="Center" Grid.Row="0" Grid.Column="0"/>
                        <DatePicker SelectedDate="{Binding StatementDate, UpdateSourceTrigger=PropertyChanged}" Grid.Row="0" Grid.Column="1" Margin="10,0,0,0"/>
                        <TextBlock Text="رصيد الكشف البنكي:" VerticalAlignment="Center" Grid.Row="2" Grid.Column="0"/>
                        <TextBox Text="{Binding StatementBalance, UpdateSourceTrigger=PropertyChanged, StringFormat=N2}" Grid.Row="2" Grid.Column="1" Margin="10,0,0,0"/>
                    </Grid>
                </StackPanel>
            </Grid>
        </Border>

        <Border DockPanel.Dock="Bottom" Background="White" CornerRadius="5" Margin="0,15,0,0" Padding="15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="ملخص العمليات" FontWeight="Bold" Margin="0,0,0,10"/>
                    <Grid Margin="10,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="5"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="5"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <TextBlock Text="إجمالي المحدد مدين:" Grid.Row="0" Grid.Column="0"/>
                        <TextBlock Text="{Binding ClearedDebitsFormatted}" Grid.Row="0" Grid.Column="1" Margin="10,0,0,0"/>
                        <TextBlock Text="إجمالي المحدد دائن:" Grid.Row="2" Grid.Column="0"/>
                        <TextBlock Text="{Binding ClearedCreditsFormatted}" Grid.Row="2" Grid.Column="1" Margin="10,0,0,0"/>
                        <TextBlock Text="الرصيد الدفتري المعدل:" Grid.Row="4" Grid.Column="0" FontWeight="Medium"/>
                        <TextBlock Text="{Binding AdjustedBookBalanceFormatted}" Grid.Row="4" Grid.Column="1" Margin="10,0,0,0" FontWeight="Medium"/>
                    </Grid>
                </StackPanel>
                <Separator Grid.Column="1" Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}"/>
                <StackPanel Grid.Column="2" HorizontalAlignment="Center">
                    <TextBlock Text="الفرق" FontWeight="Bold" HorizontalAlignment="Center"/>
                    <TextBlock Text="{Binding DifferenceFormatted}" FontSize="24" FontWeight="Bold" Margin="0,10,0,15" HorizontalAlignment="Center">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="Red"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Difference}" Value="0">
                                        <Setter Property="Foreground" Value="Green"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                    <Button Content="💾 حفظ التسوية" Style="{StaticResource PrimaryButtonStyle}" Command="{Binding SaveReconciliationCommand}"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Background="White" CornerRadius="5" Padding="15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="الحركات التي لم تتم تسويتها" FontWeight="Bold" Margin="0,0,0,10"/>
                <DataGrid ItemsSource="{Binding Transactions}" Grid.Row="1" AutoGenerateColumns="False" CanUserAddRows="False">
                    <DataGrid.Columns>
                        <DataGridCheckBoxColumn Header="تسوية" Binding="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}" Width="60"/>
                        <DataGridTextColumn Header="التاريخ" Binding="{Binding Date, StringFormat='d'}" IsReadOnly="True" Width="100"/>
                        <DataGridTextColumn Header="البيان" Binding="{Binding Description}" IsReadOnly="True" Width="*"/>
                        <DataGridTextColumn Header="مدين" Binding="{Binding DebitFormatted}" IsReadOnly="True" Width="120"/>
                        <DataGridTextColumn Header="دائن" Binding="{Binding CreditFormatted}" IsReadOnly="True" Width="120"/>
                    </DataGrid.Columns>
                </DataGrid>
                <TextBlock Text="جاري تحميل الحركات..." FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"/>
            </Grid>
        </Border>
    </DockPanel>
</UserControl>