<!-- UI/Views/BankReconciliationView.xaml -->
<!-- *** ملف جديد: واجهة عرض التسوية البنكية *** -->
<UserControl x:Class="GoodMorningFactory.UI.Views.BankReconciliationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             mc:Ignorable="d" d:DesignHeight="700" d:DesignWidth="1000"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             Background="#F5F5F5" FlowDirection="RightToLeft">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <GroupBox Header="معلومات التسوية" Grid.Row="0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Label Content="الحساب البنكي:" Grid.Column="0"/>
                <ComboBox x:Name="BankAccountComboBox" Grid.Column="1" Margin="5" DisplayMemberPath="AccountName" SelectedValuePath="Id" SelectionChanged="BankAccountComboBox_SelectionChanged"/>
                <Label Content="تاريخ كشف الحساب:" Grid.Column="2"/>
                <DatePicker x:Name="StatementDatePicker" Grid.Column="3" Margin="5" SelectedDateChanged="StatementDateOrBalanceChanged"/>
                <Label Content="رصيد نهاية الكشف:" Grid.Row="1" Grid.Column="0"/>
                <TextBox x:Name="StatementBalanceTextBox" Grid.Row="1" Grid.Column="1" Margin="5" TextChanged="StatementDateOrBalanceChanged"/>
            </Grid>
        </GroupBox>

        <DataGrid x:Name="TransactionsDataGrid" Grid.Row="1" Margin="0,10,0,0" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTemplateColumn Header="تسوية">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <CheckBox IsChecked="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="التاريخ" Binding="{Binding Date, StringFormat='d'}" IsReadOnly="True" Width="*"/>
                <DataGridTextColumn Header="البيان" Binding="{Binding Description}" IsReadOnly="True" Width="3*"/>
                <DataGridTextColumn Header="مدين" Binding="{Binding Debit, StringFormat=C}" IsReadOnly="True" Width="*"/>
                <DataGridTextColumn Header="دائن" Binding="{Binding Credit, StringFormat=C}" IsReadOnly="True" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <Border Grid.Row="2" Padding="10" Margin="0,10,0,0" BorderBrush="LightGray" BorderThickness="0,1,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Vertical" Grid.Column="0">
                    <TextBlock Text="ملخص التسوية" FontSize="14" FontWeight="Bold"/>
                    <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                        <TextBlock Text="رصيد الدفاتر:"/>
                        <TextBlock x:Name="BookBalanceTextBlock" Margin="5,0" Text="0.00"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="إجمالي المحدد للمدين:"/>
                        <TextBlock x:Name="ClearedDebitsTextBlock" Margin="5,0" Text="0.00"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="إجمالي المحدد للدائن:"/>
                        <TextBlock x:Name="ClearedCreditsTextBlock" Margin="5,0" Text="0.00"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                        <TextBlock Text="الرصيد المعدل:" FontWeight="Bold"/>
                        <TextBlock x:Name="AdjustedBalanceTextBlock" FontWeight="Bold" Margin="5,0" Text="0.00"/>
                    </StackPanel>
                </StackPanel>
                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock Text="الفرق" HorizontalAlignment="Center"/>
                    <TextBlock x:Name="DifferenceTextBlock" Text="0.00" FontSize="22" FontWeight="Bold" HorizontalAlignment="Center"/>
                    <Button Content="💾 حفظ التسوية" Padding="10,5" Margin="0,10,0,0" HorizontalAlignment="Center" Click="SaveReconciliationButton_Click"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>