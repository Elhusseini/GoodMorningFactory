<UserControl x:Class="GoodMorningFactory.UI.Views.LeaveManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:GoodMorningFactory.UI.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000"
             Background="#F5F5F5" FlowDirection="RightToLeft">

    <UserControl.DataContext>
        <vm:LeaveManagementViewModel/>
    </UserControl.DataContext>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Button Content="➕ تقديم طلب إجازة جديد" Command="{Binding AddRequestCommand}" Grid.Row="0" HorizontalAlignment="Left" Margin="5" Padding="10,5"/>

        <DataGrid ItemsSource="{Binding LeaveRequests}" SelectedItem="{Binding SelectedRequest}" Grid.Row="1" Margin="5" IsReadOnly="True" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="الموظف" Binding="{Binding Employee.FirstName}" Width="2*"/>
                <DataGridTextColumn Header="نوع الإجازة" Binding="{Binding LeaveType.Name}" Width="*"/>
                <DataGridTextColumn Header="تاريخ البدء" Binding="{Binding StartDate, StringFormat='d'}" Width="*"/>
                <DataGridTextColumn Header="تاريخ الانتهاء" Binding="{Binding EndDate, StringFormat='d'}" Width="*"/>
                <DataGridTextColumn Header="الحالة" Binding="{Binding Status}" Width="*" FontWeight="Bold"/>
                <DataGridTemplateColumn Header="إجراءات" Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="موافقة" Margin="2" Command="{Binding DataContext.ApproveRequestCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" Background="DarkSeaGreen"/>
                                <Button Content="رفض" Margin="2" Command="{Binding DataContext.RejectRequestCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" Background="IndianRed" Foreground="White"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>