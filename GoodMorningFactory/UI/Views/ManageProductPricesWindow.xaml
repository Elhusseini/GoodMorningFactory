<Window x:Class="GoodMorningFactory.UI.Views.ManageProductPricesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:GoodMorningFactory.UI.ViewModels"
        mc:Ignorable="d"
        Title="{Binding WindowTitle}" Height="600" Width="800"
        WindowStartupLocation="CenterScreen" FlowDirection="RightToLeft"
        d:DataContext="{d:DesignInstance Type=vm:ManageProductPricesViewModel, IsDesignTimeCreatable=True}">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="يمكنك تعديل الأسعار مباشرة في الجدول. السعر (0) يعني استخدام السعر الافتراضي للمنتج." TextWrapping="Wrap" Margin="5,5,5,10"/>

        <DataGrid Grid.Row="1" ItemsSource="{Binding ProductPrices}" AutoGenerateColumns="False" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="كود المنتج" Binding="{Binding Product.ProductCode}" IsReadOnly="True" Width="*"/>
                <DataGridTextColumn Header="اسم المنتج" Binding="{Binding Product.Name}" IsReadOnly="True" Width="2*"/>
                <DataGridTextColumn Header="السعر الافتراضي" Binding="{Binding Product.SalePrice, StringFormat=N2}" IsReadOnly="True" Width="*"/>
                <DataGridTextColumn Header="السعر في هذه القائمة" Binding="{Binding Price, UpdateSourceTrigger=PropertyChanged}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="حفظ التغييرات" Command="{Binding SaveCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}" MinWidth="120" Margin="5"/>
            <Button Content="إغلاق" IsCancel="True" MinWidth="100" Margin="5"/>
        </StackPanel>
    </Grid>
</Window>