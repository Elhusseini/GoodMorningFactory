<Window x:Class="GoodMorningFactory.UI.Views.AddEditQualityCheckWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:GoodMorningFactory.UI.ViewModels"
        mc:Ignorable="d"
        Title="{Binding WindowTitle}" Height="600" Width="800"
        WindowStartupLocation="CenterOwner" FlowDirection="RightToLeft">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Padding="10" BorderBrush="LightGray" BorderThickness="1" CornerRadius="5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Label Grid.Row="0" Grid.Column="0" Content="المنتج المفحوص:"/>
                <ComboBox Grid.Row="0" Grid.Column="1" Margin="5" ItemsSource="{Binding Products}" DisplayMemberPath="Name" SelectedValuePath="Id" SelectedValue="{Binding QualityCheck.ProductId}"/>

                <Label Grid.Row="0" Grid.Column="2" Content="تاريخ الفحص:"/>
                <DatePicker Grid.Row="0" Grid.Column="3" Margin="5" SelectedDate="{Binding QualityCheck.CheckDate}"/>

                <Label Grid.Row="1" Grid.Column="0" Content="نوع الفحص:"/>
                <ComboBox Grid.Row="1" Grid.Column="1" Margin="5" ItemsSource="{Binding CheckTypes}" SelectedItem="{Binding QualityCheck.CheckType}"/>

                <Label Grid.Row="1" Grid.Column="2" Content="النتيجة النهائية:"/>
                <ComboBox Grid.Row="1" Grid.Column="3" Margin="5" ItemsSource="{Binding Statuses}" SelectedItem="{Binding QualityCheck.OverallStatus}"/>
            </Grid>
        </Border>

        <DataGrid Grid.Row="1" Margin="0,10,0,0" ItemsSource="{Binding Results}" CanUserAddRows="False" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="المعيار" Binding="{Binding Parameter.Name}" IsReadOnly="True" Width="2*"/>
                <DataGridTextColumn Header="القيمة المتوقعة" Binding="{Binding Parameter.ExpectedValue}" IsReadOnly="True" Width="2*"/>
                <DataGridTextColumn Header="النتيجة الفعلية" Binding="{Binding Result.ActualValue, UpdateSourceTrigger=PropertyChanged}" Width="2*"/>
                <DataGridCheckBoxColumn Header="مطابق؟" Binding="{Binding Result.IsConforming, UpdateSourceTrigger=PropertyChanged}" Width="*"/>
                <DataGridTextColumn Header="ملاحظات" Binding="{Binding Result.Notes, UpdateSourceTrigger=PropertyChanged}" Width="3*"/>
            </DataGrid.Columns>
        </DataGrid>

        <Button Grid.Row="2" Content="💾 حفظ نتيجة الفحص" HorizontalAlignment="Left" Padding="15,5" Margin="5" Click="SaveButton_Click"/>
    </Grid>
</Window>