<UserControl x:Class="GoodMorningFactory.UI.Views.SalesOrdersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:converters="clr-namespace:GoodMorningFactory.UI.Converters"
             xmlns:vm="clr-namespace:GoodMorningFactory.UI.ViewModels"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1200"
             FlowDirection="RightToLeft"
             d:DataContext="{d:DesignInstance Type=vm:SalesOrdersViewModel, IsDesignTimeCreatable=True}">

    <UserControl.DataContext>
        <vm:SalesOrdersViewModel/>
    </UserControl.DataContext>

    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding LoadDataCommand}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>
    <UserControl.Resources>
        <converters:EnumToDescriptionConverter x:Key="EnumToDescriptionConverter"/>
        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.5"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Padding="10" Background="#F5F5F5" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal">
                <Button Content="إنشاء أمر بيع" Command="{Binding AddOrderCommand}" Margin="0,0,20,0"/>
                <TextBlock Text="بحث:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <TextBox Width="200" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged, Delay=300}" VerticalContentAlignment="Center"/>
                <TextBlock Text="الحالة:" VerticalAlignment="Center" Margin="10,0,5,0"/>
                <ComboBox Width="150" ItemsSource="{Binding StatusFilters}" SelectedItem="{Binding SelectedStatusFilter}" DisplayMemberPath="Name"/>
            </StackPanel>
        </Border>

        <DataGrid Grid.Row="1" ItemsSource="{Binding Orders}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="رقم الأمر" Binding="{Binding SalesOrderNumber}"/>
                <DataGridTextColumn Header="تاريخ الأمر" Binding="{Binding OrderDate, StringFormat='yyyy/MM/dd'}"/>
                <DataGridTextColumn Header="اسم العميل" Binding="{Binding CustomerName}" Width="*"/>
                <DataGridTextColumn Header="المبلغ الإجمالي" Binding="{Binding TotalAmountFormatted}"/>
                <DataGridTextColumn Header="تاريخ الشحن المتوقع" Binding="{Binding ExpectedShipDate, StringFormat='yyyy/MM/dd'}"/>
                <DataGridTemplateColumn Header="حالة الأمر">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding OrderStatus, Converter={StaticResource EnumToDescriptionConverter}}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn Header="حالة الشحن">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding ShippingStatus, Converter={StaticResource EnumToDescriptionConverter}}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn Header="حالة الفوترة">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding InvoicingStatus, Converter={StaticResource EnumToDescriptionConverter}}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn Header="إجراءات" Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="تعديل" Background="Orange" Command="{Binding DataContext.EditOrderCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}"/>
                                <Button Content="إلغاء" Background="IndianRed" Command="{Binding DataContext.CancelOrderCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}"/>
                                <Button Content="إنشاء أمر عمل" Background="Purple" Command="{Binding DataContext.CreateWorkOrderCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}"/>
                                <Button Content="إنشاء شحنة" Background="SeaGreen" Command="{Binding DataContext.CreateShipmentCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}"/>
                                <Button Content="إنشاء فاتورة" Background="DodgerBlue" Command="{Binding DataContext.CreateInvoiceCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <Button Content="&lt; السابق" Command="{Binding PreviousPageCommand}"/>
            <TextBlock Text="{Binding PageInfo}" Margin="10,0" VerticalAlignment="Center"/>
            <Button Content="التالي &gt;" Command="{Binding NextPageCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>