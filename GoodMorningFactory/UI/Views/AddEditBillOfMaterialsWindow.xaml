<!-- UI/Views/AddEditBillOfMaterialsWindow.xaml -->
<!-- نافذة إضافة وتعديل قائمة المكونات -->
<Window x:Class="GoodMorningFactory.UI.Views.AddEditBillOfMaterialsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="إنشاء / تعديل قائمة مكونات" Height="600" Width="800"
        WindowStartupLocation="CenterOwner" FlowDirection="RightToLeft">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <GroupBox Header="معلومات القائمة" Grid.Row="0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>

                <Label Grid.Row="0" Grid.Column="0" Content="المنتج النهائي:"/>
                <ComboBox x:Name="FinishedGoodComboBox" Grid.Row="0" Grid.Column="1" Margin="5" DisplayMemberPath="Name" SelectedValuePath="Id"/>

                <Label Grid.Row="1" Grid.Column="0" Content="الوصف:"/>
                <TextBox x:Name="DescriptionTextBox" Grid.Row="1" Grid.Column="1" Margin="5"/>
            </Grid>
        </GroupBox>

        <GroupBox Header="المواد الخام المطلوبة" Grid.Row="1" Margin="0,10,0,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <StackPanel Orientation="Horizontal" Margin="5">
                    <TextBlock Text="بحث عن مادة خام:" VerticalAlignment="Center" Margin="5"/>
                    <TextBox x:Name="SearchMaterialTextBox" Width="300" KeyUp="SearchMaterialTextBox_KeyUp"/>
                </StackPanel>
                <DataGrid x:Name="BomItemsDataGrid" Grid.Row="1" CanUserAddRows="False" AutoGenerateColumns="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="المادة الخام" Binding="{Binding RawMaterialName}" IsReadOnly="True" Width="2*"/>
                        <DataGridTextColumn Header="الكمية" Binding="{Binding Quantity, StringFormat=N4}" Width="*"/>
                        <DataGridTemplateColumn>
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="❌" Click="RemoveItem_Click" Background="Transparent" BorderThickness="0" Foreground="Red"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </GroupBox>

        <Button Content="💾 حفظ القائمة" Grid.Row="2" HorizontalAlignment="Left" Padding="15,5" Margin="5" Click="SaveButton_Click"/>
    </Grid>
</Window>