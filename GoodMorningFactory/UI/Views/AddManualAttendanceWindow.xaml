<Window x:Class="GoodMorningFactory.UI.Views.AddManualAttendanceWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d" 
        Title="تسجيل حضور/انصراف يدوي" Height="300" Width="450"
        WindowStartupLocation="CenterOwner" FlowDirection="RightToLeft">
    <Grid Margin="15">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Grid.Column="0" Content="الموظف:" VerticalAlignment="Center"/>
        <ComboBox Grid.Row="0" Grid.Column="1" Margin="5" ItemsSource="{Binding Employees}" SelectedItem="{Binding SelectedEmployee}" DisplayMemberPath="FirstName"/>

        <Label Grid.Row="1" Grid.Column="0" Content="التاريخ:" VerticalAlignment="Center"/>
        <DatePicker Grid.Row="1" Grid.Column="1" Margin="5" SelectedDate="{Binding AttendanceDate}"/>

        <Label Grid.Row="2" Grid.Column="0" Content="الوقت (HH:mm):" VerticalAlignment="Center"/>
        <TextBox Grid.Row="2" Grid.Column="1" Margin="5" Text="{Binding AttendanceTime, UpdateSourceTrigger=PropertyChanged}"/>

        <StackPanel Grid.Row="3" Grid.Column="1" Orientation="Horizontal" Margin="5">
            <Button Content="✅ تسجيل حضور" Width="120" Margin="5" Padding="5" Command="{Binding SignInCommand}" Click="CloseButton_Click"/>
            <Button Content="❌ تسجيل انصراف" Width="120" Margin="5" Padding="5" Command="{Binding SignOutCommand}" Click="CloseButton_Click"/>
        </StackPanel>
    </Grid>
</Window>