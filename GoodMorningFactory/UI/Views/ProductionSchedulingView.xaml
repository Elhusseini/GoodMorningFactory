<!-- UI/Views/ProductionSchedulingView.xaml -->
<!-- *** ملف جديد: واجهة عرض جدولة الإنتاج *** -->
<UserControl x:Class="GoodMorningFactory.UI.Views.ProductionSchedulingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000"
             Background="#F5F5F5" FlowDirection="RightToLeft">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="جدول أوامر العمل المفتوحة" FontSize="18" FontWeight="Bold" Margin="5"/>

        <DataGrid x:Name="SchedulingDataGrid" Grid.Row="1" Margin="5" IsReadOnly="True" AutoGenerateColumns="False" CanUserAddRows="False">
            <!-- استخدام Style Trigger لتلوين الصفوف المتأخرة -->
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsDelayed}" Value="True">
                            <Setter Property="Background" Value="MistyRose"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>

            <DataGrid.Columns>
                <DataGridTextColumn Header="رقم الأمر" Binding="{Binding WorkOrderNumber}" Width="*"/>
                <DataGridTextColumn Header="المنتج" Binding="{Binding FinishedGood.Name}" Width="2*"/>
                <DataGridTextColumn Header="الكمية" Binding="{Binding QuantityToProduce}" Width="*"/>
                <DataGridTextColumn Header="تاريخ البدء المخطط" Binding="{Binding PlannedStartDate, StringFormat='d'}" Width="*"/>
                <DataGridTextColumn Header="تاريخ الانتهاء المخطط" Binding="{Binding PlannedEndDate, StringFormat='d'}" Width="*"/>
                <DataGridTextColumn Header="الحالة" Binding="{Binding Status}" Width="*" FontWeight="Bold"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>