<UserControl x:Class="GoodMorningFactory.UI.Views.IncomeStatementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:GoodMorningFactory.UI.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="800"
             FlowDirection="RightToLeft" Background="#F5F5F5"
             d:DataContext="{d:DesignInstance Type=vm:IncomeStatementViewModel, IsDesignTimeCreatable=True}">

    <UserControl.DataContext>
        <vm:IncomeStatementViewModel/>
    </UserControl.DataContext>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="تقرير قائمة الدخل (الأرباح والخسائر)" FontSize="20" FontWeight="Bold" Margin="5"/>

        <Border Grid.Row="1" Background="White" CornerRadius="5" Padding="10" Margin="5" BorderBrush="#DDD" BorderThickness="1">
            <StackPanel Orientation="Horizontal">
                <Label Content="من تاريخ:" VerticalAlignment="Center"/>
                <DatePicker Width="120" VerticalAlignment="Center" SelectedDate="{Binding FromDate}"/>
                <Label Content="إلى تاريخ:" VerticalAlignment="Center" Margin="10,0,0,0"/>
                <DatePicker Width="120" VerticalAlignment="Center" SelectedDate="{Binding ToDate}"/>
                <Button Content="📊 عرض التقرير" Margin="15,0" Padding="10,5" Command="{Binding GenerateReportCommand}"/>
            </StackPanel>
        </Border>

        <ScrollViewer Grid.Row="2" Margin="5" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <TextBlock Text="{Binding ReportDateRange}" HorizontalAlignment="Center" Margin="0,10" FontWeight="SemiBold" FontSize="14"/>

                <TextBlock Text="الإيرادات" FontSize="16" FontWeight="Bold" Margin="0,10,0,5" Foreground="DarkGreen"/>
                <DataGrid ItemsSource="{Binding Revenues}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" HeadersVisibility="Column">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="رقم الحساب" Binding="{Binding AccountNumber}" Width="*"/>
                        <DataGridTextColumn Header="اسم الحساب" Binding="{Binding AccountName}" Width="3*"/>
                        <DataGridTextColumn Header="الرصيد" Binding="{Binding Balance, StringFormat=N2}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
                <Border BorderBrush="Gray" BorderThickness="0,1,0,0" Padding="5" Margin="0,0,0,10">
                    <Grid>
                        <TextBlock Text="إجمالي الإيرادات" FontWeight="Bold"/>
                        <TextBlock Text="{Binding TotalRevenue, StringFormat=N2}" FontWeight="Bold" HorizontalAlignment="Left" Margin="250,0,0,0"/>
                    </Grid>
                </Border>

                <TextBlock Text="المصاريف" FontSize="16" FontWeight="Bold" Margin="0,20,0,5" Foreground="DarkRed"/>
                <DataGrid ItemsSource="{Binding Expenses}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" HeadersVisibility="Column">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="رقم الحساب" Binding="{Binding AccountNumber}" Width="*"/>
                        <DataGridTextColumn Header="اسم الحساب" Binding="{Binding AccountName}" Width="3*"/>
                        <DataGridTextColumn Header="الرصيد" Binding="{Binding Balance, StringFormat=N2}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
                <Border BorderBrush="Gray" BorderThickness="0,1,0,0" Padding="5" Margin="0,0,0,10">
                    <Grid>
                        <TextBlock Text="إجمالي المصاريف" FontWeight="Bold"/>
                        <TextBlock Text="{Binding TotalExpenses, StringFormat=N2}" FontWeight="Bold" HorizontalAlignment="Left" Margin="250,0,0,0"/>
                    </Grid>
                </Border>

                <Border BorderBrush="Black" BorderThickness="0,2,0,0" Margin="0,20,0,0" Padding="5">
                    <Grid>
                        <TextBlock Text="صافي الربح / (الخسارة)" FontWeight="ExtraBold" FontSize="16"/>
                        <TextBlock Text="{Binding NetIncome, StringFormat=N2}" FontWeight="ExtraBold" FontSize="16" HorizontalAlignment="Left" Margin="250,0,0,0"/>
                    </Grid>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>