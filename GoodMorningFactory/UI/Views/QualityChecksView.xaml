<UserControl x:Class="GoodMorningFactory.UI.Views.QualityChecksView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:GoodMorningFactory.UI.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             Background="#F5F5F5" FlowDirection="RightToLeft">

    <UserControl.DataContext>
        <vm:QualityChecksViewModel/>
    </UserControl.DataContext>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Button Content="➕ تسجيل فحص جديد" Command="{Binding AddCheckCommand}" Grid.Row="0" HorizontalAlignment="Left" Margin="5" Padding="10,5"/>

        <DataGrid ItemsSource="{Binding QualityChecks}" SelectedItem="{Binding SelectedQualityCheck}" Grid.Row="1" Margin="5" IsReadOnly="True" AutoGenerateColumns="False" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="تاريخ الفحص" Binding="{Binding CheckDate, StringFormat='g'}" Width="*"/>
                <DataGridTextColumn Header="المنتج" Binding="{Binding Product.Name}" Width="2*"/>
                <DataGridTextColumn Header="نوع الفحص" Binding="{Binding CheckType}" Width="*"/>
                <DataGridTextColumn Header="النتيجة النهائية" Binding="{Binding OverallStatus}" Width="*" FontWeight="Bold"/>
                <DataGridTemplateColumn Header="إجراءات" Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="عرض التفاصيل" Command="{Binding DataContext.ViewCheckCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>