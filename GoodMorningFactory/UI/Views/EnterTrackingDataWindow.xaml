<Window x:Class="GoodMorningFactory.UI.Views.EnterTrackingDataWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:converters="clr-namespace:GoodMorningFactory.UI.Converters"
        mc:Ignorable="d"
        Title="إدخال بيانات التتبع" Height="450" Width="400"
        WindowStartupLocation="CenterOwner" FlowDirection="RightToLeft">
    <Window.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </Window.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="{Binding HeaderText}" FontSize="18" FontWeight="Bold"/>
            <TextBlock Text="{Binding InstructionsText}" Margin="0,5,0,10"/>
        </StackPanel>

        <Grid Grid.Row="1">
            <StackPanel Visibility="{Binding IsSerialEntryVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
                <Label Content="أدخل الرقم التسلسلي واضغط Enter:"/>
                <TextBox Text="{Binding CurrentSerial, UpdateSourceTrigger=PropertyChanged}">
                    <TextBox.InputBindings>
                        <KeyBinding Key="Enter" Command="{Binding AddSerialCommand}"/>
                    </TextBox.InputBindings>
                </TextBox>
                <ListBox ItemsSource="{Binding SerialNumbers}" Margin="0,10,0,0" Height="200"/>
            </StackPanel>

            <StackPanel Visibility="{Binding IsLotEntryVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
                <Label Content="رقم الدفعة:"/>
                <TextBox Text="{Binding LotNumber, UpdateSourceTrigger=PropertyChanged}"/>
                <Label Content="تاريخ الصلاحية (اختياري):"/>
                <DatePicker SelectedDate="{Binding ExpiryDate}"/>
            </StackPanel>
        </Grid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="تأكيد" Style="{DynamicResource PrimaryButtonStyle}" Command="{Binding ConfirmCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"/>
            <Button Content="إلغاء" IsCancel="True" Style="{DynamicResource SecondaryButtonStyle}" Margin="10,0,0,0"/>
        </StackPanel>
    </Grid>
</Window>