<UserControl x:Class="GoodMorningFactory.UI.Views.AccountsReceivableView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:s="clr-namespace:GoodMorningFactory.Core.Services"
             xmlns:vm="clr-namespace:GoodMorningFactory.UI.ViewModels"
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="1100"
             Background="#F5F5F5" FlowDirection="RightToLeft"
             d:DataContext="{d:DesignInstance Type=vm:AccountsReceivableViewViewModel, IsDesignTimeCreatable=True}">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="تقرير أعمار الذمم المدينة" FontSize="18" FontWeight="Bold" Margin="5"/>
        <Button Grid.Row="1" Content="🔄 تحديث" Command="{Binding RefreshCommand}" HorizontalAlignment="Left" Margin="5,0,0,10"/>

        <DataGrid ItemsSource="{Binding AgingReportItems}" Grid.Row="2" IsReadOnly="True" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="العميل" Binding="{Binding CustomerName}" Width="2*"/>
                <DataGridTextColumn Header="رقم الفاتورة" Binding="{Binding InvoiceNumber}" Width="*"/>
                <DataGridTextColumn Header="تاريخ الاستحقاق" Binding="{Binding DueDate, StringFormat='d'}" Width="*"/>

                <DataGridTemplateColumn Header="إجمالي الفاتورة" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock TextAlignment="Right" Margin="4,0">
                                <Run Text="{Binding TotalAmount, StringFormat=N2}"/>
                                <Run Text="{x:Static s:AppSettings.DefaultCurrencySymbol}"/>
                            </TextBlock>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn Header="الرصيد المستحق" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock TextAlignment="Right" Margin="4,0" FontWeight="Bold">
                                <Run Text="{Binding BalanceDue, StringFormat=N2}"/>
                                <Run Text="{x:Static s:AppSettings.DefaultCurrencySymbol}"/>
                            </TextBlock>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn Header="0-30 يوم" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock TextAlignment="Right" Margin="4,0">
                                <Run Text="{Binding Bucket0_30, StringFormat=N2, FallbackValue=0}"/>
                                <Run Text="{x:Static s:AppSettings.DefaultCurrencySymbol}"/>
                            </TextBlock>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn Header="31-60 يوم" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock TextAlignment="Right" Margin="4,0">
                                <Run Text="{Binding Bucket31_60, StringFormat=N2, FallbackValue=0}"/>
                                <Run Text="{x:Static s:AppSettings.DefaultCurrencySymbol}"/>
                            </TextBlock>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn Header="61-90 يوم" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock TextAlignment="Right" Margin="4,0">
                                <Run Text="{Binding Bucket61_90, StringFormat=N2, FallbackValue=0}"/>
                                <Run Text="{x:Static s:AppSettings.DefaultCurrencySymbol}"/>
                            </TextBlock>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn Header="أكثر من 90 يوم" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock TextAlignment="Right" Margin="4,0">
                                <Run Text="{Binding BucketOver90, StringFormat=N2, FallbackValue=0}"/>
                                <Run Text="{x:Static s:AppSettings.DefaultCurrencySymbol}"/>
                            </TextBlock>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn Header="إجراءات" Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="تسجيل دفعة" Margin="2" Command="{Binding DataContext.RecordPaymentCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}"/>
                                <Button Content="إرسال تذكير" Margin="2" Command="{Binding DataContext.SendReminderCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" Background="#FFC107"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>