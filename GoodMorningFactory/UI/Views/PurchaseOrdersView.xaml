<UserControl x:Class="GoodMorningFactory.UI.Views.PurchaseOrdersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GoodMorningFactory.UI.Views"
             xmlns:models="clr-namespace:GoodMorningFactory.Data.Models"
             xmlns:vm="clr-namespace:GoodMorningFactory.UI.ViewModels"
             xmlns:converters="clr-namespace:GoodMorningFactory.UI.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1200"
             FlowDirection="RightToLeft">

    <UserControl.DataContext>
        <vm:PurchaseOrdersViewModel/>
    </UserControl.DataContext>

    <UserControl.Resources>
        <converters:EnumToDescriptionConverter x:Key="EnumToDescriptionConverter"/>
        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.5"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Padding="10" Background="#F5F5F5" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal">
                <Button Content="إنشاء أمر شراء" Command="{Binding AddPurchaseOrderCommand}" Margin="0,0,20,0"/>
                <TextBlock Text="بحث:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Width="200" VerticalContentAlignment="Center">
                    <TextBox.InputBindings>
                        <KeyBinding Key="Enter" Command="{Binding SearchCommand}" />
                    </TextBox.InputBindings>
                </TextBox>
                <TextBlock Text="الحالة:" VerticalAlignment="Center" Margin="10,0,5,0"/>
                <ComboBox ItemsSource="{Binding StatusFilters}" SelectedItem="{Binding SelectedStatusFilter}" Width="150">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Converter={StaticResource EnumToDescriptionConverter}}"/>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>
                <Button Content="🔄" Command="{Binding RefreshCommand}" Margin="10,0,0,0" ToolTip="تحديث القائمة"/>
            </StackPanel>
        </Border>

        <DataGrid Grid.Row="1" ItemsSource="{Binding Orders}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="رقم الأمر" Binding="{Binding PurchaseOrderNumber}"/>
                <DataGridTextColumn Header="تاريخ الأمر" Binding="{Binding OrderDate, StringFormat='yyyy/MM/dd'}"/>
                <DataGridTextColumn Header="اسم المورد" Binding="{Binding SupplierName}" Width="*"/>
                <DataGridTextColumn Header="المبلغ الإجمالي" Binding="{Binding TotalAmountFormatted}"/>
                <DataGridTextColumn Header="تاريخ التسليم المتوقع" Binding="{Binding ExpectedDeliveryDate, StringFormat='yyyy/MM/dd'}"/>
                <DataGridTextColumn Header="حالة الأمر" Binding="{Binding Status, Converter={StaticResource EnumToDescriptionConverter}}"/>
                <DataGridTextColumn Header="حالة الاستلام" Binding="{Binding ReceiptStatus, Converter={StaticResource EnumToDescriptionConverter}}"/>
                <DataGridTextColumn Header="حالة الفوترة" Binding="{Binding InvoicingStatus, Converter={StaticResource EnumToDescriptionConverter}}"/>

                <DataGridTemplateColumn Header="إجراءات" Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="تعديل" Background="Orange" Style="{StaticResource ActionButtonStyle}"
                                        Command="{Binding DataContext.EditCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" 
                                        CommandParameter="{Binding}"/>
                                <Button Content="إلغاء" Background="IndianRed" Style="{StaticResource ActionButtonStyle}"
                                        Command="{Binding DataContext.CancelCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" 
                                        CommandParameter="{Binding}"/>
                                <Button Content="طباعة" Background="Gray" Style="{StaticResource ActionButtonStyle}"
                                        Command="{Binding DataContext.PrintCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" 
                                        CommandParameter="{Binding}"/>
                                <Button Content="تسجيل استلام" Background="SeaGreen" Style="{StaticResource ActionButtonStyle}"
                                        Command="{Binding DataContext.ReceiveGoodsCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" 
                                        CommandParameter="{Binding}"/>
                                <Button Content="إنشاء فاتورة" Background="DodgerBlue" Style="{StaticResource ActionButtonStyle}"
                                        Command="{Binding DataContext.CreateInvoiceCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" 
                                        CommandParameter="{Binding}"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <Button Content="السابق" Command="{Binding PreviousPageCommand}"/>
            <TextBlock Text="{Binding PageInfo}" Margin="10,0" VerticalAlignment="Center"/>
            <Button Content="التالي" Command="{Binding NextPageCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>