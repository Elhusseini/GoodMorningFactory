<!-- UI/Views/PurchaseOrdersView.xaml -->
<!-- *** تحديث: تمت إضافة زر "إنشاء فاتورة" مع Triggers *** -->
<UserControl x:Class="GoodMorningFactory.UI.Views.PurchaseOrdersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000"
             Background="#F5F5F5" FlowDirection="RightToLeft">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ToolBarTray Grid.Row="0">
            <ToolBar>
                <Button Content="➕ إنشاء أمر شراء جديد" Margin="5" Padding="5" Click="AddPurchaseOrderButton_Click"/>
                <Separator/>
                <TextBlock Text="بحث:" VerticalAlignment="Center" Margin="5"/>
                <TextBox x:Name="SearchTextBox" Width="200" KeyUp="Filter_KeyUp"/>
                <TextBlock Text="الحالة:" VerticalAlignment="Center" Margin="5"/>
                <ComboBox x:Name="StatusFilterComboBox" Width="120" SelectionChanged="Filter_Changed"/>
            </ToolBar>
        </ToolBarTray>

        <DataGrid x:Name="PurchaseOrdersDataGrid" Grid.Row="1" Margin="5" IsReadOnly="True" AutoGenerateColumns="False" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="رقم الأمر" Binding="{Binding PurchaseOrderNumber}" Width="*"/>
                <DataGridTextColumn Header="المورد" Binding="{Binding Supplier.Name}" Width="2*"/>
                <DataGridTextColumn Header="تاريخ الأمر" Binding="{Binding OrderDate, StringFormat='d'}" Width="*"/>
                <DataGridTextColumn Header="تاريخ التسليم المتوقع" Binding="{Binding ExpectedDeliveryDate, StringFormat='d'}" Width="*"/>
                <DataGridTextColumn Header="الإجمالي" Binding="{Binding TotalAmount, StringFormat=C}" Width="*"/>
                <DataGridTextColumn Header="الحالة" Binding="{Binding Status}" Width="*" FontWeight="Bold"/>
                <DataGridTemplateColumn Header="إجراءات" Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="تعديل" Margin="2" Click="EditButton_Click"/>
                                <Button Content="طباعة" Margin="2" Click="PrintButton_Click" Background="LightGray"/>
                                <Button Content="تسجيل استلام" Margin="2" Click="ReceiveGoodsButton_Click" Background="LightGreen"/>
								<!-- *** بداية التحديث: إضافة زر إنشاء فاتورة *** -->
								<Button Content="إنشاء فاتورة" Margin="2" Click="CreateInvoiceButton_Click" Background="#FFC107">
									<Button.Style>
										<Style TargetType="Button">
											<Setter Property="Visibility" Value="Collapsed"/>
											<Style.Triggers>
												<!-- يظهر فقط إذا تم استلام البضاعة (جزئياً أو كلياً) -->
												<DataTrigger Binding="{Binding Status}" Value="PartiallyReceived">
													<Setter Property="Visibility" Value="Visible"/>
												</DataTrigger>
												<DataTrigger Binding="{Binding Status}" Value="FullyReceived">
													<Setter Property="Visibility" Value="Visible"/>
												</DataTrigger>
											</Style.Triggers>
										</Style>
									</Button.Style>
								</Button>
								<!-- *** نهاية التحديث *** -->
                                <Button Content="إلغاء" Margin="2" Click="CancelButton_Click" Background="OrangeRed" Foreground="White"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <Border Grid.Row="2" BorderThickness="0,1,0,0" BorderBrush="LightGray" Padding="5" Margin="0,5,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <Button x:Name="PreviousPageButton" Content="&lt; السابق" Margin="5,0" Click="PreviousPageButton_Click"/>
                    <Button x:Name="NextPageButton" Content="التالي &gt;" Margin="5,0" Click="NextPageButton_Click"/>
                </StackPanel>
                <TextBlock x:Name="PageInfoTextBlock" Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Right"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>