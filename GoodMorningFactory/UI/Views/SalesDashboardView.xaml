<!-- UI/Views/SalesDashboardView.xaml -->
<!-- *** الكود الكامل لتصميم لوحة معلومات المبيعات مع جميع الإضافات *** -->
<UserControl x:Class="GoodMorningFactory.UI.Views.SalesDashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1200"
             Background="#F5F5F5" FlowDirection="RightToLeft">
    <UserControl.Resources>
        <Style x:Key="FunnelBorderStyle" TargetType="Border">
            <Setter Property="BorderBrush" Value="DodgerBlue"/>
            <Setter Property="BorderThickness" Value="0,0,0,2"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Margin" Value="5"/>
        </Style>
    </UserControl.Resources>
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- عنوان الشاشة -->
            <TextBlock Grid.Row="0" Text="لوحة معلومات المبيعات" FontSize="24" FontWeight="Bold" Margin="5"/>

            <!-- الجزء العلوي: بطاقات المؤشرات -->
            <UniformGrid Rows="1" Columns="4" Grid.Row="1">
                <Border Style="{StaticResource DashboardCardStyle}" Background="#007BFF">
                    <StackPanel>
                        <TextBlock Text="إجمالي مبيعات الشهر" Style="{StaticResource DashboardCardTitleStyle}"/>
                        <TextBlock Text="{Binding TotalSalesThisMonth, StringFormat={}{0:C}}" Style="{StaticResource DashboardCardValueStyle}"/>
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource DashboardCardStyle}" Background="#28A745">
                    <StackPanel>
                        <TextBlock Text="أوامر البيع الجديدة (الشهر)" Style="{StaticResource DashboardCardTitleStyle}"/>
                        <TextBlock Text="{Binding NewOrdersThisMonth}" Style="{StaticResource DashboardCardValueStyle}"/>
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource DashboardCardStyle}" Background="#6f42c1">
                    <StackPanel>
                        <TextBlock Text="متوسط قيمة الأمر" Style="{StaticResource DashboardCardTitleStyle}"/>
                        <TextBlock Text="{Binding AverageOrderValue, StringFormat={}{0:C}}" Style="{StaticResource DashboardCardValueStyle}"/>
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource DashboardCardStyle}" Background="#FFC107">
                    <StackPanel>
                        <TextBlock Text="عروض أسعار للمتابعة" Style="{StaticResource DashboardCardTitleStyle}"/>
                        <TextBlock Text="{Binding FollowUpQuotationsCount}" Style="{StaticResource DashboardCardValueStyle}"/>
                    </StackPanel>
                </Border>
            </UniformGrid>

            <!-- الجزء الأوسط: الرسوم البيانية -->
            <Grid Grid.Row="2" Margin="0,10,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="3*"/>
                    <ColumnDefinition Width="2*"/>
                </Grid.ColumnDefinitions>

                <GroupBox Header="أداء المبيعات الشهري (آخر 6 أشهر)" Grid.Column="0" Margin="5" FontSize="14" FontWeight="Bold">
                    <lvc:CartesianChart MinHeight="250" Series="{Binding MonthlySalesSeries}" LegendLocation="None">
                        <lvc:CartesianChart.AxisX>
                            <lvc:Axis Labels="{Binding MonthLabels}"/>
                        </lvc:CartesianChart.AxisX>
                        <lvc:CartesianChart.AxisY>
                            <lvc:Axis x:Name="SalesAxisY" Title="إجمالي المبيعات"/>
                        </lvc:CartesianChart.AxisY>
                    </lvc:CartesianChart>
                </GroupBox>

                <GroupBox Header="المبيعات حسب فئة المنتج" Grid.Column="1" Margin="5" FontSize="14" FontWeight="Bold">
                    <lvc:CartesianChart MinHeight="250" Series="{Binding SalesByCategorySeries}" LegendLocation="None">
                        <lvc:CartesianChart.AxisX>
                            <lvc:Axis Labels="{Binding CategoryLabels}"/>
                        </lvc:CartesianChart.AxisX>
                        <lvc:CartesianChart.AxisY>
                            <lvc:Axis x:Name="CategorySalesAxisY" Title="إجمالي المبيعات"/>
                        </lvc:CartesianChart.AxisY>
                    </lvc:CartesianChart>
                </GroupBox>
            </Grid>

            <!-- الجزء السفلي: القوائم والاختصارات -->
            <Grid Grid.Row="3" Margin="0,10,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <GroupBox Header="أفضل العملاء مبيعاً" Grid.Column="0" Margin="5" FontSize="14" FontWeight="Bold">
                    <ItemsControl ItemsSource="{Binding TopCustomers}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding}" Margin="5" FontSize="14"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </GroupBox>
                <GroupBox Header="أفضل المنتجات مبيعاً" Grid.Column="1" Margin="5" FontSize="14" FontWeight="Bold">
                    <ItemsControl ItemsSource="{Binding TopProducts}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding}" Margin="5" FontSize="14"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </GroupBox>

                <StackPanel Grid.Column="2">
                    <!-- قمع المبيعات -->
                    <GroupBox Header="قمع المبيعات (هذا الشهر)" Margin="5" FontSize="14" FontWeight="Bold">
                        <StackPanel Margin="10">
                            <Border Style="{StaticResource FunnelBorderStyle}">
                                <Grid>
                                    <TextBlock Text="عروض الأسعار"/>
                                    <TextBlock Text="{Binding QuotationsCount}" HorizontalAlignment="Left"/>
                                </Grid>
                            </Border>
                            <Border Style="{StaticResource FunnelBorderStyle}" Margin="20,5,5,5">
                                <Grid>
                                    <TextBlock Text="أوامر البيع"/>
                                    <TextBlock Text="{Binding OrdersCount}" HorizontalAlignment="Left"/>
                                </Grid>
                            </Border>
                            <Border Style="{StaticResource FunnelBorderStyle}" Margin="40,5,5,5">
                                <Grid>
                                    <TextBlock Text="الفواتير"/>
                                    <TextBlock Text="{Binding InvoicesCount}" HorizontalAlignment="Left"/>
                                </Grid>
                            </Border>
                        </StackPanel>
                    </GroupBox>
                    <!-- اختصارات سريعة -->
                    <GroupBox Header="اختصارات سريعة" Margin="5" FontSize="14" FontWeight="Bold">
                        <StackPanel>
                            <Button Content="➕ إضافة عميل جديد" Margin="5" Padding="5" Click="AddCustomer_Click"/>
                            <Button Content="➕ إنشاء عرض سعر جديد" Margin="5" Padding="5" Click="AddQuotation_Click"/>
                            <Button Content="➕ إنشاء أمر بيع جديد" Margin="5" Padding="5" Click="AddOrder_Click"/>
                        </StackPanel>
                    </GroupBox>
                </StackPanel>
            </Grid>
        </Grid>
    </ScrollViewer>
</UserControl>