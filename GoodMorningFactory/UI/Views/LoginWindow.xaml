<Window x:Class="GoodMorningFactory.UI.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewModels="clr-namespace:GoodMorningFactory.UI.ViewModels"
        xmlns:helpers="clr-namespace:GoodMorningFactory.UI.Helpers"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=viewModels:LoginViewModel}"
        Title="تسجيل الدخول - نظام مصنع صباح الخير" Height="400" Width="400"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize" FlowDirection="RightToLeft">
    <Grid>
        <StackPanel VerticalAlignment="Center" Margin="30">
            <TextBlock Text="تسجيل الدخول" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20"/>

            <Label Content="اسم المستخدم:"/>
            <TextBox Margin="0,0,0,10" Padding="5"
                     Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" />

            <Label Content="كلمة المرور:"/>
            <PasswordBox Margin="0,0,0,10" Padding="5"
                         helpers:PasswordBoxHelper.BindPassword="True"
                         helpers:PasswordBoxHelper.BoundPassword="{Binding Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Text="{Binding ErrorMessage}" 
                       Foreground="Red" 
                       TextWrapping="Wrap" 
                       Margin="0,0,0,10"
                       Visibility="{Binding ErrorMessage, Converter={StaticResource NotNullToBooleanConverter}}"/>

            <Button Content="دخول" Padding="10" FontSize="16" IsDefault="True"
                    Command="{Binding LoginCommand}" 
                    IsEnabled="{Binding !IsLoggingIn}"/>
        </StackPanel>

        <ProgressBar IsIndeterminate="True" 
                     Height="5" VerticalAlignment="Bottom"
                     Visibility="{Binding IsLoggingIn, Converter={StaticResource BooleanToVisibilityConverter}}"/>
    </Grid>
</Window>