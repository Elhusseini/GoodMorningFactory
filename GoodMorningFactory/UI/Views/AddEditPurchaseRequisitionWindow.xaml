<!-- UI/Views/AddEditPurchaseRequisitionWindow.xaml -->
<!-- نافذة إضافة وتعديل طلب شراء -->
<Window x:Class="GoodMorningFactory.UI.Views.AddEditPurchaseRequisitionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        mc:Ignorable="d" Title="إنشاء طلب شراء جديد" Height="600" Width="800"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        WindowStartupLocation="CenterOwner" FlowDirection="RightToLeft">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <GroupBox Header="معلومات الطلب" Grid.Row="0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>

                <Label Grid.Row="0" Grid.Column="0" Content="مقدم الطلب:"/>
                <TextBox x:Name="RequesterTextBox" Grid.Row="0" Grid.Column="1" Margin="5"/>
                <Label Grid.Row="1" Grid.Column="0" Content="القسم:"/>
                <TextBox x:Name="DepartmentTextBox" Grid.Row="1" Grid.Column="1" Margin="5"/>
                <Label Grid.Row="2" Grid.Column="0" Content="الغرض من الشراء:"/>
                <TextBox x:Name="PurposeTextBox" Grid.Row="2" Grid.Column="1" Margin="5"/>
            </Grid>
        </GroupBox>

        <DataGrid x:Name="ItemsDataGrid" Grid.Row="1" Margin="0,10,0,0" CanUserAddRows="True" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="الوصف" Binding="{Binding Description}" Width="3*"/>
                <DataGridTextColumn Header="الكمية" Binding="{Binding Quantity}" Width="*"/>
                <DataGridTextColumn Header="وحدة القياس" Binding="{Binding UnitOfMeasure}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <Button Grid.Row="2" Content="💾 حفظ الطلب" HorizontalAlignment="Left" Padding="15,5" Margin="5" Click="SaveButton_Click"/>
    </Grid>
</Window>