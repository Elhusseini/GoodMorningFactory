<Window x:Class="GoodMorningFactory.UI.Views.AddEditPurchaseInvoiceWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GoodMorningFactory.UI.Views"
        xmlns:vm="clr-namespace:GoodMorningFactory.UI.ViewModels"
        mc:Ignorable="d"
        Title="{Binding Title}" Height="700" Width="900"
        WindowStartupLocation="CenterScreen" FlowDirection="RightToLeft">
	<Grid Margin="15">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>

		<GroupBox Header="معلومات الفاتورة" Grid.Row="0" Padding="10" Margin="0,0,0,10">
			<Grid>
				<Grid.RowDefinitions>
					<RowDefinition Height="Auto"/>
					<RowDefinition Height="Auto"/>
				</Grid.RowDefinitions>
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="Auto"/>
					<ColumnDefinition Width="*"/>
					<ColumnDefinition Width="Auto"/>
					<ColumnDefinition Width="*"/>
				</Grid.ColumnDefinitions>
				<Label Grid.Row="0" Grid.Column="0" Content="المورد:" VerticalAlignment="Center"/>
				<ComboBox Grid.Row="0" Grid.Column="1" ItemsSource="{Binding AllSuppliers}" SelectedItem="{Binding SelectedSupplier}" DisplayMemberPath="Name" Margin="5"/>
				<Label Grid.Row="0" Grid.Column="2" Content="رقم فاتورة المورد:" VerticalAlignment="Center"/>
				<TextBox Grid.Row="0" Grid.Column="3" Text="{Binding InvoiceNumber}" Margin="5"/>
				<Label Grid.Row="1" Grid.Column="0" Content="تاريخ الفاتورة:" VerticalAlignment="Center"/>
				<DatePicker Grid.Row="1" Grid.Column="1" SelectedDate="{Binding InvoiceDate}" Margin="5"/>
				<Label Grid.Row="1" Grid.Column="2" Content="تاريخ الاستحقاق:" VerticalAlignment="Center"/>
				<DatePicker Grid.Row="1" Grid.Column="3" SelectedDate="{Binding DueDate}" Margin="5"/>
			</Grid>
		</GroupBox>

		<GroupBox Header="بحث وإضافة منتج" Grid.Row="1">
			<StackPanel Orientation="Horizontal">
				<TextBlock Text="ابحث بالاسم أو الكود:" VerticalAlignment="Center" Margin="5"/>
				<TextBox Width="300" Margin="5" Text="{Binding ProductSearchText, UpdateSourceTrigger=PropertyChanged, Delay=200}">
					<TextBox.InputBindings>
						<KeyBinding Key="Enter" Command="{Binding SearchAndAddItemCommand}"/>
					</TextBox.InputBindings>
				</TextBox>
			</StackPanel>
		</GroupBox>

		<DataGrid Grid.Row="2" ItemsSource="{Binding Items}" AutoGenerateColumns="False" CanUserAddRows="False" Margin="0,10,0,0">
			<DataGrid.Columns>
				<DataGridTemplateColumn Header="المنتج/المادة" Width="3*">
					<DataGridTemplateColumn.CellTemplate>
						<DataTemplate>
							<ComboBox ItemsSource="{Binding DataContext.AllProducts, RelativeSource={RelativeSource AncestorType=Window}}"
                                      SelectedValue="{Binding ProductId, UpdateSourceTrigger=PropertyChanged}"
                                      SelectedValuePath="Id"
                                      DisplayMemberPath="Name"/>
						</DataTemplate>
					</DataGridTemplateColumn.CellTemplate>
				</DataGridTemplateColumn>
				<DataGridTextColumn Header="الكمية" Binding="{Binding Quantity, UpdateSourceTrigger=PropertyChanged}" Width="*"/>
				<DataGridTextColumn Header="سعر الوحدة" Binding="{Binding UnitPrice, StringFormat=N2, UpdateSourceTrigger=PropertyChanged}" Width="*"/>
				<DataGridTextColumn Header="الإجمالي الفرعي" Binding="{Binding Subtotal, StringFormat=N2}" Width="*" FontWeight="Bold" IsReadOnly="True"/>
				<DataGridTemplateColumn Width="Auto">
					<DataGridTemplateColumn.CellTemplate>
						<DataTemplate>
							<Button Content="❌" Command="{Binding DataContext.RemoveItemCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding}" Background="Transparent" BorderThickness="0" Foreground="Red"/>
						</DataTemplate>
					</DataGridTemplateColumn.CellTemplate>
				</DataGridTemplateColumn>
			</DataGrid.Columns>
		</DataGrid>

		<Grid Grid.Row="3" Margin="0,10,0,0">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*"/>
				<ColumnDefinition Width="Auto"/>
			</Grid.ColumnDefinitions>
			<StackPanel Grid.Column="0" Orientation="Horizontal" HorizontalAlignment="Left">
				<Button Content="حفظ الفاتورة" Command="{Binding SaveCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}" Width="120" Margin="5"/>
				<Button Content="إلغاء" Width="100" Margin="5" IsCancel="True"/>
				<Button Content="➕ إضافة بند" Command="{Binding AddItemCommand}" Width="100" Margin="20,5,5,5"/>
			</StackPanel>
			<StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
				<TextBlock Text="إجمالي الفاتورة:" FontSize="18" FontWeight="Bold" VerticalAlignment="Center"/>
				<TextBlock Text="{Binding TotalAmountText}" FontSize="18" FontWeight="Bold" Foreground="#3182CE" Margin="10,0"/>
			</StackPanel>
		</Grid>
	</Grid>
</Window>