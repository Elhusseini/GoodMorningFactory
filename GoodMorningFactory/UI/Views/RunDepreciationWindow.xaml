<Window x:Class="GoodMorningFactory.UI.Views.RunDepreciationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:GoodMorningFactory.UI.ViewModels"
        mc:Ignorable="d"
        Title="احتساب إهلاك الأصول الثابتة" Height="450" Width="650"
        FlowDirection="RightToLeft" WindowStartupLocation="CenterOwner">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </Window.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="احتساب قيد الإهلاك الشهري" Style="{DynamicResource TitleTextBlockStyle}" Grid.Row="0" Margin="0,0,0,10"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <Label Content="احتساب الإهلاك حتى نهاية شهر:"/>
            <ComboBox ItemsSource="{Binding Months}" SelectedItem="{Binding SelectedMonth}" Width="80"/>
            <ComboBox ItemsSource="{Binding Years}" SelectedItem="{Binding SelectedYear}" Width="100" Margin="10,0,0,0"/>
            <Button Content="عرض قيد الإهلاك المقترح" Margin="20,0,0,0" Command="{Binding CalculateCommand}"/>
        </StackPanel>

        <GroupBox Header="تفاصيل قيد الإهلاك المقترح" Grid.Row="2" Visibility="{Binding HasProposedVoucher, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Margin="5" FontWeight="Bold" Text="{Binding ProposedVoucher.Description}"/>
                <DataGrid Grid.Row="1" ItemsSource="{Binding ProposedVoucher.JournalVoucherItems}" IsReadOnly="True" AutoGenerateColumns="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="اسم الحساب" Binding="{Binding Account.DisplayName}" Width="*"/>
                        <DataGridTextColumn Header="مدين" Binding="{Binding Debit, StringFormat='N2'}" Width="100"/>
                        <DataGridTextColumn Header="دائن" Binding="{Binding Credit, StringFormat='N2'}" Width="100"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </GroupBox>

        <TextBlock Grid.Row="2" Text="جاري الاحتساب..." HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="16" FontWeight="Bold" Visibility="{Binding IsCalculating, Converter={StaticResource BooleanToVisibilityConverter}}"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="ترحيل القيد" Style="{DynamicResource PrimaryButtonStyle}" Command="{Binding PostVoucherCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"/>
            <Button Content="إغلاق" Style="{DynamicResource SecondaryButtonStyle}" Margin="10,0,0,0" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>