<Window x:Class="GoodMorningFactory.UI.Views.AddEditPriceListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding WindowTitle}" Height="250" Width="450"
        WindowStartupLocation="CenterOwner" FlowDirection="RightToLeft">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="اسم القائمة:"/>
            <TextBox Text="{Binding PriceList.Name, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,10"/>

            <TextBlock Text="الوصف:"/>
            <TextBox Text="{Binding PriceList.Description, UpdateSourceTrigger=PropertyChanged}" Margin="0,5"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
            <Button Content="💾 حفظ" IsDefault="True" Command="{Binding SaveCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}" MinWidth="100" Margin="5"/>
            <Button Content="إلغاء" IsCancel="True" MinWidth="100" Margin="5"/>
        </StackPanel>
    </Grid>
</Window>