<UserControl x:Class="GoodMorningFactory.UI.Views.InventoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:GoodMorningFactory.UI.ViewModels"
             xmlns:converters="clr-namespace:GoodMorningFactory.UI.Converters"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="1200"
             FlowDirection="RightToLeft"
             d:DataContext="{d:DesignInstance Type=vm:InventoryStatusViewModel, IsDesignTimeCreatable=True}">

	<i:Interaction.Triggers>
		<i:EventTrigger EventName="Loaded">
			<i:InvokeCommandAction Command="{Binding LoadDataCommand}" />
		</i:EventTrigger>
	</i:Interaction.Triggers>

	<UserControl.Resources>
		<converters:EnumToDescriptionConverter x:Key="EnumToDescriptionConverter"/>
	</UserControl.Resources>

	<Grid Margin="10">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>

		<Border Grid.Row="0" Margin="0,0,0,10">
			<WrapPanel>
				<StackPanel Orientation="Horizontal" Margin="5">
					<Label Content="بحث:" VerticalAlignment="Center"/>
					<TextBox Width="200" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged, Delay=300}"/>
				</StackPanel>
				<StackPanel Orientation="Horizontal" Margin="5">
					<Label Content="المخزن الرئيسي:" VerticalAlignment="Center"/>
					<ComboBox Width="150" ItemsSource="{Binding WarehouseFilters}" SelectedItem="{Binding SelectedWarehouse}" DisplayMemberPath="Name"/>
				</StackPanel>
				<StackPanel Orientation="Horizontal" Margin="5">
					<Label Content="الفئة:" VerticalAlignment="Center"/>
					<ComboBox Width="150" ItemsSource="{Binding CategoryFilters}" SelectedItem="{Binding SelectedCategory}" DisplayMemberPath="Name"/>
				</StackPanel>
				<StackPanel Orientation="Horizontal" Margin="5">
					<Label Content="الحالة:" VerticalAlignment="Center"/>
					<ComboBox Width="120" ItemsSource="{Binding StatusFilters}" SelectedItem="{Binding SelectedStatus}">
						<ComboBox.ItemTemplate>
							<DataTemplate>
								<TextBlock Text="{Binding Converter={StaticResource EnumToDescriptionConverter}}"/>
							</DataTemplate>
						</ComboBox.ItemTemplate>
					</ComboBox>
				</StackPanel>
			</WrapPanel>
		</Border>

		<Grid Grid.Row="1">
			<Grid.RowDefinitions>
				<RowDefinition Height="2*"/>
				<RowDefinition Height="Auto"/>
				<RowDefinition Height="1*"/>
			</Grid.RowDefinitions>

			<DataGrid x:Name="MasterGrid" Grid.Row="0" ItemsSource="{Binding InventoryItems}"
                      SelectedItem="{Binding SelectedInventoryItem}"
                      IsReadOnly="True" AutoGenerateColumns="False" VerticalScrollBarVisibility="Auto">
				<DataGrid.Columns>
					<DataGridTextColumn Header="كود المنتج" Binding="{Binding ProductCode}" Width="Auto"/>
					<DataGridTextColumn Header="اسم المنتج" Binding="{Binding ProductName}" Width="2*"/>
					<DataGridTextColumn Header="الفئة" Binding="{Binding CategoryName}" Width="*"/>
					<DataGridTextColumn Header="إجمالي الكمية" Binding="{Binding TotalQuantityOnHand}" Width="Auto" FontWeight="Bold"/>
					<DataGridTextColumn Header="إجمالي المحجوز" Binding="{Binding TotalQuantityReserved}" Width="Auto"/>
					<DataGridTextColumn Header="إجمالي المتاح" Binding="{Binding TotalQuantityAvailable}" Width="Auto" FontWeight="Bold"/>
					<DataGridTextColumn Header="آخر سعر شراء" Binding="{Binding LastPurchasePriceFormatted}" Width="*"/>
					<DataGridTextColumn Header="متوسط التكلفة" Binding="{Binding AverageCostFormatted}" Width="*"/>
					<DataGridTextColumn Header="إجمالي القيمة" Binding="{Binding TotalStockValueFormatted}" Width="*" FontWeight="Bold"/>
					<DataGridTextColumn Header="الحالة" Binding="{Binding Status, Converter={StaticResource EnumToDescriptionConverter}}" Width="*"/>
				</DataGrid.Columns>
			</DataGrid>

			<GridSplitter Grid.Row="1" Height="5" HorizontalAlignment="Stretch" Background="LightGray" VerticalAlignment="Center"/>

			<GroupBox Header="تفاصيل أرصدة المنتج المحدد في المواقع" Grid.Row="2" Margin="0,5,0,0" FontWeight="Bold">
				<DataGrid ItemsSource="{Binding SelectedInventoryItem.LocationDetails}"
                          IsReadOnly="True" AutoGenerateColumns="False" VerticalScrollBarVisibility="Auto" CanUserAddRows="False">
					<DataGrid.Columns>
						<DataGridTextColumn Header="المخزن الرئيسي" Binding="{Binding WarehouseName}" Width="*"/>
						<DataGridTextColumn Header="الموقع الفرعي" Binding="{Binding StorageLocationName}" Width="2*"/>
						<DataGridTextColumn Header="الكمية المتاحة" Binding="{Binding QuantityOnHand}" Width="*"/>
						<DataGridTextColumn Header="الكمية المحجوزة" Binding="{Binding QuantityReserved}" Width="*"/>
						<DataGridTextColumn Header="مستوى إعادة الطلب" Binding="{Binding ReorderLevel}" Width="*"/>
						<DataGridTemplateColumn Header="إجراءات" Width="Auto">
							<DataGridTemplateColumn.CellTemplate>
								<DataTemplate>
									<StackPanel Orientation="Horizontal">
										<Button Content="تعديل سريع" Margin="2"
                                                Command="{Binding DataContext.QuickAdjustCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}"/>
									</StackPanel>
								</DataTemplate>
							</DataGridTemplateColumn.CellTemplate>
						</DataGridTemplateColumn>
					</DataGrid.Columns>
				</DataGrid>
			</GroupBox>

		</Grid>
		<StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
			<Button Content="السابق" Command="{Binding PreviousPageCommand}"/>
			<TextBlock Text="{Binding PageInfo}" Margin="10,0" VerticalAlignment="Center"/>
			<Button Content="التالي" Command="{Binding NextPageCommand}"/>
		</StackPanel>
	</Grid>
</UserControl>