<Window x:Class="GoodMorningFactory.UI.Views.AddEditBudgetWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        mc:Ignorable="d" 
        Title="{Binding WindowTitle}" Height="700" Width="1200"
        FlowDirection="RightToLeft" WindowStartupLocation="CenterOwner">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="بيانات الموازنة الأساسية" Style="{DynamicResource TitleTextBlockStyle}"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Label Content="اسم الموازنة:" Grid.Row="0" Grid.Column="0"/>
                <TextBox Text="{Binding Budget.Name, UpdateSourceTrigger=PropertyChanged}" Grid.Row="0" Grid.Column="1" Margin="5"/>

                <Label Content="سنة الموازنة:" Grid.Row="0" Grid.Column="2" Margin="10,0,0,0"/>
                <ComboBox ItemsSource="{Binding AvailableYears}" SelectedItem="{Binding Budget.Year, UpdateSourceTrigger=PropertyChanged}" Grid.Row="0" Grid.Column="3" Margin="5"/>

                <Label Content="الوصف:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center"/>
                <TextBox Text="{Binding Budget.Description, UpdateSourceTrigger=PropertyChanged}" Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="3" Margin="5"/>
            </Grid>
        </StackPanel>

        <DataGrid ItemsSource="{Binding BudgetDetails}" Grid.Row="1" AutoGenerateColumns="False" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="رقم الحساب" Binding="{Binding Account.AccountNumber}" IsReadOnly="True" FontWeight="Bold"/>
                <DataGridTextColumn Header="اسم الحساب" Binding="{Binding Account.AccountName}" IsReadOnly="True" Width="200" FontWeight="Bold"/>
                <DataGridTextColumn Header="يناير" Binding="{Binding Month1Amount, StringFormat=N2, UpdateSourceTrigger=PropertyChanged}"/>
                <DataGridTextColumn Header="فبراير" Binding="{Binding Month2Amount, StringFormat=N2, UpdateSourceTrigger=PropertyChanged}"/>
                <DataGridTextColumn Header="مارس" Binding="{Binding Month3Amount, StringFormat=N2, UpdateSourceTrigger=PropertyChanged}"/>
                <DataGridTextColumn Header="أبريل" Binding="{Binding Month4Amount, StringFormat=N2, UpdateSourceTrigger=PropertyChanged}"/>
                <DataGridTextColumn Header="مايو" Binding="{Binding Month5Amount, StringFormat=N2, UpdateSourceTrigger=PropertyChanged}"/>
                <DataGridTextColumn Header="يونيو" Binding="{Binding Month6Amount, StringFormat=N2, UpdateSourceTrigger=PropertyChanged}"/>
                <DataGridTextColumn Header="يوليو" Binding="{Binding Month7Amount, StringFormat=N2, UpdateSourceTrigger=PropertyChanged}"/>
                <DataGridTextColumn Header="أغسطس" Binding="{Binding Month8Amount, StringFormat=N2, UpdateSourceTrigger=PropertyChanged}"/>
                <DataGridTextColumn Header="سبتمبر" Binding="{Binding Month9Amount, StringFormat=N2, UpdateSourceTrigger=PropertyChanged}"/>
                <DataGridTextColumn Header="أكتوبر" Binding="{Binding Month10Amount, StringFormat=N2, UpdateSourceTrigger=PropertyChanged}"/>
                <DataGridTextColumn Header="نوفمبر" Binding="{Binding Month11Amount, StringFormat=N2, UpdateSourceTrigger=PropertyChanged}"/>
                <DataGridTextColumn Header="ديسمبر" Binding="{Binding Month12Amount, StringFormat=N2, UpdateSourceTrigger=PropertyChanged}"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="حفظ الموازنة" Style="{DynamicResource PrimaryButtonStyle}" Command="{Binding SaveCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"/>
            <Button Content="إلغاء" Style="{DynamicResource SecondaryButtonStyle}" Margin="10,0,0,0" IsCancel="True"/>
        </StackPanel>

        <ProgressBar Grid.Row="1" IsIndeterminate="{Binding IsLoading}" VerticalAlignment="Center" Height="20" Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"/>
    </Grid>
</Window>