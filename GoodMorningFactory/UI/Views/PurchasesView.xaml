<!-- UI/Views/PurchasesView.xaml -->
<!-- *** الكود الكامل لواجهة فواتير الموردين مع الفلاتر والأزرار الجديدة *** -->
<UserControl x:Class="GoodMorningFactory.UI.Views.PurchasesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="1000"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             Background="#F5F5F5" FlowDirection="RightToLeft">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- شريط الأدوات مع الفلاتر -->
        <ToolBarTray Grid.Row="0">
            <ToolBar>
                <Button Content="➕ تسجيل فاتورة جديدة" Margin="5" Padding="5" Click="AddPurchaseButton_Click"/>
                <Separator/>
                <TextBlock Text="بحث:" VerticalAlignment="Center" Margin="5"/>
                <TextBox x:Name="SearchTextBox" Width="200" KeyUp="Filter_KeyUp"/>
                <TextBlock Text="الحالة:" VerticalAlignment="Center" Margin="5"/>
                <ComboBox x:Name="StatusFilterComboBox" Width="120" SelectionChanged="Filter_Changed"/>
            </ToolBar>
        </ToolBarTray>

        <!-- جدول عرض فواتير الموردين -->
        <DataGrid x:Name="PurchasesDataGrid" Grid.Row="1" Margin="0,10,0,0" IsReadOnly="True" AutoGenerateColumns="False" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="رقم الفاتورة" Binding="{Binding InvoiceNumber}" Width="*"/>
                <DataGridTextColumn Header="المورد" Binding="{Binding Supplier.Name}" Width="2*"/>
                <DataGridTextColumn Header="تاريخ الفاتورة" Binding="{Binding PurchaseDate, StringFormat='d'}" Width="*"/>
                <DataGridTextColumn Header="تاريخ الاستحقاق" Binding="{Binding DueDate, StringFormat='d'}" Width="*"/>
                <DataGridTextColumn Header="الإجمالي" Binding="{Binding TotalAmount, StringFormat=C}" Width="*"/>
                <DataGridTextColumn Header="المدفوع" Binding="{Binding AmountPaid, StringFormat=C}" Width="*"/>
                <DataGridTextColumn Header="الرصيد المستحق" Binding="{Binding BalanceDue, StringFormat=C}" Width="*" FontWeight="Bold"/>
                <DataGridTextColumn Header="الحالة" Binding="{Binding Status}" Width="*" FontWeight="Bold"/>
                <!-- عمود الإجراءات مع الأزرار الجديدة -->
                <DataGridTemplateColumn Header="إجراءات" Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="تسجيل دفعة" Margin="2" Click="RecordPaymentButton_Click" Background="DarkSeaGreen"/>
                                <Button Content="إنشاء إشعار مدين" Margin="2" Click="CreateDebitNoteButton_Click" Background="LightCoral"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>