<UserControl x:Class="GoodMorningFactory.UI.Views.PurchasesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GoodMorningFactory.UI.Views"
             xmlns:converters="clr-namespace:GoodMorningFactory.UI.Converters"
             xmlns:vm="clr-namespace:GoodMorningFactory.UI.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="950"
             Background="#F4F7FC"
             FlowDirection="RightToLeft"
             FontFamily="Segoe UI"
             d:DataContext="{d:DesignInstance Type=vm:PurchasesViewModel, IsDesignTimeCreatable=True}">
    <UserControl.Resources>
        <converters:EnumToDescriptionConverter x:Key="EnumToDescriptionConverter"/>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="إدارة فواتير الموردين" FontSize="28" FontWeight="Bold" Foreground="#1A202C" Margin="0,0,0,20"/>

        <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="15" Margin="0,0,0,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="بحث:" VerticalAlignment="Center" Margin="5"/>
                    <TextBox Width="200" Margin="5" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged, Delay=300}"/>
                    <TextBlock Text="الحالة:" VerticalAlignment="Center" Margin="10,0,0,0"/>
                    <ComboBox Width="150" Margin="5" 
                              ItemsSource="{Binding StatusFilters}"
                              SelectedItem="{Binding SelectedStatusFilter}"
                              DisplayMemberPath="Name"/>
                </StackPanel>
                <Button Grid.Column="1" Content="فاتورة جديدة" Command="{Binding AddPurchaseCommand}" Background="#38A169" Foreground="White" Padding="10,5" Cursor="Hand"/>
            </Grid>
        </Border>

        <DataGrid Grid.Row="2" 
                  ItemsSource="{Binding Purchases}"
                  Margin="0" 
                  IsReadOnly="True" 
                  AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="رقم الفاتورة" Binding="{Binding InvoiceNumber}" Width="*"/>
                <DataGridTextColumn Header="المورد" Binding="{Binding SupplierName}" Width="*"/>
                <DataGridTextColumn Header="تاريخ الفاتورة" Binding="{Binding PurchaseDate, StringFormat=d}" Width="*"/>
                <DataGridTextColumn Header="تاريخ الاستحقاق" Binding="{Binding DueDate, StringFormat=d}" Width="*"/>
                <DataGridTextColumn Header="الإجمالي" Binding="{Binding TotalAmountFormatted}" Width="*" FontWeight="SemiBold"/>
                <DataGridTextColumn Header="المدفوع" Binding="{Binding AmountPaidFormatted}" Width="*"/>
                <DataGridTextColumn Header="الرصيد المستحق" Binding="{Binding BalanceDueFormatted}" Width="*" FontWeight="Bold"/>
                <DataGridTemplateColumn Header="الحالة" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Status, Converter={StaticResource EnumToDescriptionConverter}}" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn Header="إجراءات" Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="تعديل" Command="{Binding DataContext.EditPurchaseCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" Margin="2"/>
                                <Button Content="تسجيل دفعة" Command="{Binding DataContext.RecordPaymentCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" Margin="2"/>
                                <Button Content="إنشاء إشعار مدين" Command="{Binding DataContext.CreateDebitNoteCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" Margin="2"/>
                                <Button Content="طباعة" Command="{Binding DataContext.PrintCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" Margin="2"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
            <Button Content="→ السابق" Margin="5,0" Command="{Binding PreviousPageCommand}"/>
            <TextBlock VerticalAlignment="Center" FontWeight="SemiBold" Text="{Binding PageInfo}"/>
            <Button Content="التالي ←" Margin="5,0" Command="{Binding NextPageCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>