<UserControl x:Class="GoodMorningFactory.UI.Views.ShipmentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GoodMorningFactory.UI.Views"
             xmlns:converters="clr-namespace:GoodMorningFactory.UI.Converters"
             xmlns:vm="clr-namespace:GoodMorningFactory.UI.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000" FlowDirection="RightToLeft"
             d:DataContext="{d:DesignInstance Type=vm:ShipmentsViewViewModel, IsDesignTimeCreatable=True}">
    <UserControl.Resources>
        <converters:EnumToDescriptionConverter x:Key="EnumToDescriptionConverter"/>
    </UserControl.Resources>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="إدارة الشحنات" FontSize="24" FontWeight="Bold" Margin="0,0,0,10"/>

        <Border Grid.Row="1" BorderBrush="LightGray" BorderThickness="0,0,0,1" Padding="0,5,0,10" Margin="0,0,0,10">
            <WrapPanel VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal" Margin="5,2">
                    <TextBlock Text="بحث:" VerticalAlignment="Center" Margin="10,0,5,0"/>
                    <TextBox Width="250" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged, Delay=300}"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="5,2">
                    <TextBlock Text="الحالة:" VerticalAlignment="Center" Margin="10,0,5,0"/>
                    <ComboBox Width="150" ItemsSource="{Binding StatusFilters}" SelectedItem="{Binding SelectedStatusFilter}" DisplayMemberPath="Name"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="5,2">
                    <TextBlock Text="تاريخ الشحن:" VerticalAlignment="Center" Margin="10,0,5,0"/>
                    <DatePicker Width="120" SelectedDate="{Binding FromDate}"/>
                    <TextBlock Text="-" VerticalAlignment="Center" Margin="5,0"/>
                    <DatePicker Width="120" SelectedDate="{Binding ToDate}"/>
                </StackPanel>
            </WrapPanel>
        </Border>

        <DataGrid ItemsSource="{Binding Shipments}" Grid.Row="2" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="رقم الشحنة" Binding="{Binding ShipmentNumber}" Width="*"/>
                <DataGridTextColumn Header="تاريخ الشحن" Binding="{Binding ShipmentDate, StringFormat='yyyy/MM/dd'}" Width="*"/>
                <DataGridTextColumn Header="أمر البيع" Binding="{Binding SalesOrderNumber}" Width="*"/>
                <DataGridTextColumn Header="العميل" Binding="{Binding CustomerName}" Width="2*"/>
                <DataGridTextColumn Header="الناقل" Binding="{Binding Carrier}" Width="*"/>
                <DataGridTextColumn Header="رقم التتبع" Binding="{Binding TrackingNumber}" Width="*"/>
                <DataGridTemplateColumn Header="الحالة" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Status, Converter={StaticResource EnumToDescriptionConverter}}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn Header="إجراءات" Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="✏️" Command="{Binding DataContext.EditShipmentCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" ToolTip="تعديل بيانات الشحن"/>
                                <Button Content="📦" Command="{Binding DataContext.PrintPackingSlipCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" ToolTip="طباعة بوليصة الشحن"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
            <Button Content="&lt; السابق" Command="{Binding PreviousPageCommand}"/>
            <TextBlock Text="{Binding PageInfo}" VerticalAlignment="Center" Margin="10,0"/>
            <Button Content="التالي &gt;" Command="{Binding NextPageCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
