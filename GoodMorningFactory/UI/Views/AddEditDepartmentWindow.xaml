<Window x:Class="GoodMorningFactory.UI.Views.AddEditDepartmentWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="إدارة الأقسام" Height="350" Width="600"
        WindowStartupLocation="CenterScreen"
        FlowDirection="RightToLeft"
        Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Row="0" Grid.ColumnSpan="2" Background="#FF3A3A3A" Padding="10" Margin="0,0,0,15">
            <TextBlock Text="{Binding WindowTitle}" Foreground="White"
                       FontSize="16" FontWeight="Bold"
                       HorizontalAlignment="Center"/>
        </Border>

        <Label Grid.Row="1" Grid.Column="0" Content="كود القسم:"/>
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Department.Id}" IsReadOnly="True" Background="LightGray"/>

        <Label Grid.Row="2" Grid.Column="0" Content="اسم القسم (*):"/>
        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Department.Name, UpdateSourceTrigger=PropertyChanged}"/>

        <Label Grid.Row="3" Grid.Column="0" Content="الوصف:" VerticalAlignment="Top"/>
        <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding Department.Description, UpdateSourceTrigger=PropertyChanged}"
                 TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" Height="80"/>

        <StackPanel Grid.Row="4" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,15,0,0">
            <Button Content="حفظ" Width="120" IsDefault="True" Command="{Binding SaveCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"/>
            <Button Content="إلغاء" Width="120" IsCancel="True" Margin="10,0,0,0"/>
        </StackPanel>
    </Grid>
</Window>