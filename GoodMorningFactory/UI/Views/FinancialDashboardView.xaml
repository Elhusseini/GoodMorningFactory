<!-- UI/Views/FinancialDashboardView.xaml -->
<!-- *** ملف جديد: واجهة عرض لوحة المعلومات المالية *** -->
<UserControl x:Class="GoodMorningFactory.UI.Views.FinancialDashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1200"
             Background="#F5F5F5" FlowDirection="RightToLeft">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="لوحة المعلومات المالية" FontSize="24" FontWeight="Bold" Margin="5"/>

            <!-- الجزء العلوي: بطاقات المؤشرات الرئيسية -->
            <UniformGrid Rows="1" Columns="3" Grid.Row="1">
                <Border Style="{StaticResource DashboardCardStyle}" Background="#007BFF">
                    <StackPanel>
                        <TextBlock Text="إجمالي الأصول" Style="{StaticResource DashboardCardTitleStyle}"/>
                        <TextBlock Text="{Binding TotalAssets, StringFormat=C}" Style="{StaticResource DashboardCardValueStyle}"/>
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource DashboardCardStyle}" Background="#DC3545">
                    <StackPanel>
                        <TextBlock Text="إجمالي الخصوم" Style="{StaticResource DashboardCardTitleStyle}"/>
                        <TextBlock Text="{Binding TotalLiabilities, StringFormat=C}" Style="{StaticResource DashboardCardValueStyle}"/>
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource DashboardCardStyle}" Background="#17A2B8">
                    <StackPanel>
                        <TextBlock Text="إجمالي حقوق الملكية" Style="{StaticResource DashboardCardTitleStyle}"/>
                        <TextBlock Text="{Binding TotalEquity, StringFormat=C}" Style="{StaticResource DashboardCardValueStyle}"/>
                    </StackPanel>
                </Border>
            </UniformGrid>

            <!-- الجزء السفلي: الرسم البياني والمؤشرات الإضافية -->
            <Grid Grid.Row="2" Margin="0,10,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="3*"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>

                <GroupBox Header="الأداء المالي الشهري (آخر 6 أشهر)" Grid.Column="0" Margin="5" FontSize="14" FontWeight="Bold">
                    <lvc:CartesianChart Series="{Binding MonthlyPerformanceSeries}" LegendLocation="Bottom">
                        <lvc:CartesianChart.AxisX>
                            <lvc:Axis Title="الأشهر" Labels="{Binding MonthLabels}"/>
                        </lvc:CartesianChart.AxisX>
                        <lvc:CartesianChart.AxisY>
                            <lvc:Axis Title="المبلغ" LabelFormatter="{Binding Path=Y, StringFormat=C}"/>
                        </lvc:CartesianChart.AxisY>
                    </lvc:CartesianChart>
                </GroupBox>

                <StackPanel Grid.Column="1" Margin="5">
                    <Border Style="{StaticResource DashboardCardStyle}" Background="#28A745" Height="100">
                        <StackPanel>
                            <TextBlock Text="صافي الربح (العام الحالي)" Style="{StaticResource DashboardCardTitleStyle}"/>
                            <TextBlock Text="{Binding NetProfitLossYTD, StringFormat=C}" Style="{StaticResource DashboardCardValueStyle}"/>
                        </StackPanel>
                    </Border>
                    <Border Style="{StaticResource DashboardCardStyle}" Background="#6f42c1" Height="100">
                        <StackPanel>
                            <TextBlock Text="إجمالي الذمم المدينة" Style="{StaticResource DashboardCardTitleStyle}"/>
                            <TextBlock Text="{Binding AccountsReceivable, StringFormat=C}" Style="{StaticResource DashboardCardValueStyle}"/>
                        </StackPanel>
                    </Border>
                    <Border Style="{StaticResource DashboardCardStyle}" Background="#ff8c00" Height="100">
                        <StackPanel>
                            <TextBlock Text="إجمالي الذمم الدائنة" Style="{StaticResource DashboardCardTitleStyle}"/>
                            <TextBlock Text="{Binding AccountsPayable, StringFormat=C}" Style="{StaticResource DashboardCardValueStyle}"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>
        </Grid>
    </ScrollViewer>
</UserControl>